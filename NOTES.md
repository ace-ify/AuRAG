# Decisions Log

Running record of decisions and the reasoning behind them, kept so context isn't lost as the repo grows. Newest at the bottom.

## 2026-07-08 — Phase 1 kickoff

**Repo structure**: `backend/`, `agents/`, `retrieval/`, `evaluation/`, `telemetry/`, `ingestion/`, `infra/`, `frontend/`, `data/`. `retrieval/` and `evaluation/` were split out of `backend/` (originally proposed as `backend/app/retrieval` and `backend/app/evaluation`) because all four sub-agents call into them independently — keeping them top-level avoids implying they're backend-API-internal. `telemetry/` was split out of `ingestion/`/`agents/` because the Proactive Intelligence Layer (Section 8b) runs in parallel to the query path per the Section 4 architecture diagram, not inside either.

**Redis+RQ worker + watchdog file-watcher location**: kept inside `ingestion/workers/`, not a separate top-level `workers/`. Reasoning: the Section 4 architecture diagram shows "Ingestion + mem0" as a single box — the watcher/queue is the continuous-mode entry point into the same ingestion pipeline (Section 8, feature #1), not an independent architectural layer the way retrieval/evaluation/telemetry are.

**LangGraph state schema shape**: flat shared TypedDict/Pydantic state (`user_query`, `intent`, `routing_confidence`, `retrieved_context`, `graph_paths`, `agent_response`, `citations`, `ragas_scores`, `session_id`, `messages`), passed as-is between Supervisor and all sub-agents.
**Why**: directly supports answer-trail visualization (Section 9 #2) and per-answer RAGAS scoring (Section 8a) as first-class fields, and is simplest to reason about across 4 sub-agents.
**How to apply**: `agents/state.py` is the single source of truth for this shape — every sub-agent reads/writes only the fields relevant to it, no nested per-agent sub-state.

**RAGAS thresholds + runtime behavior**: ≥0.7 = pass (green), 0.5–0.7 = amber, <0.5 = red, on all three metrics (faithfulness, context precision, answer relevancy). When faithfulness <0.7 specifically, show an inline warning banner on the chat answer itself (not just the dashboard).
**Why**: gives judges a visible self-policing moment (Section 14 risk: "make the RAGAS machinery visible, not just present in the backend") without adding retry latency risk during a live demo.
**How to apply**: `evaluation/` owns the threshold constants; the frontend reads the score + a `low_faithfulness` flag rather than re-deriving thresholds client-side.

**Node unique keys**: added generated ids for `Document.id`, `Chunk.id`, `Person.person_id` — the three node types where PRD Section 6 listed properties but no primary key. All other node types use the key PRD Section 6 already specified (`Equipment.tag_id`, `WorkOrder.id`, `FailureEvent.id`, `RegulatoryClause.clause_id`, `Procedure.id`).
**Why**: idempotent re-ingestion (MERGE on a stable key) requires a unique key per node type; natural keys for these three (source_file, content-hash, name) risk collisions or don't handle file updates cleanly, which matters for the Section 14 file-hash dedup requirement in Phase 2's continuous ingestion.
**How to apply**: `infra/neo4j/schema.cypher` and the seed/ingestion loaders always generate these ids rather than relying on natural properties as MERGE keys.

**Section 8 vs. Section 9 scope**: Section 9 asks to "confirm which 1-2 differentiators ship," but Section 8 ("no cutting for time") and the user's explicit ground rules took precedence at the start of the build. This early note is now superseded by the 2026-07-12 Phase 9 product-scope decision below: GraphRAG comparison is deferred from the product UI, while answer-trail visualization and proactive telemetry remain the live product proof.

**Chunk vector index**: deferred to Phase 3 (hybrid retrieval), not created in Phase 1's schema script. Reasoning: Neo4j's native vector index requires a fixed embedding dimension, and Section 5's tech stack table doesn't name an embedding model (only Cohere for *rerank* is specified) — that's a Phase 3 decision, not a Phase 1 blocker since Phase 1 only needs node/relationship structure + constraints live.

**Scanned/handwritten OCR test sample**: user is supplying a real scanned document rather than an AI-generated synthetic one — will be dropped into `data/scanned/` once provided.

**P&ID sample**: sourcing a real public sample P&ID PDF (per Section 7's "real where possible" principle for reference/context data) rather than generating one.

## 2026-07-08 — Phase 1 build complete (pending live DB test)

Repo scaffolded; schema + seed Cypher written and syntax-validated (`ingestion/loader/load_seed.py`'s statement splitter dry-run against all files — quote/comment handling confirmed correct, including the regulatory-clause text that contains a semicolon inside a string literal). `neo4j` + `python-dotenv` installed locally. Full inventory:

- `infra/neo4j/schema.cypher` — 8 uniqueness constraints + 4 lookup indexes.
- `infra/neo4j/seed/01_equipment.cypher` — 15 Equipment + 3 HAS_PART.
- `infra/neo4j/seed/02_personnel.cypher` — 6 Person.
- `infra/neo4j/seed/03_documents_and_procedures.cypher` — 4 Document (SOP, incident log, P&ID, scanned log) + 1 Procedure + 2 GOVERNS.
- `infra/neo4j/seed/04_failure_events.cypher` — 6 FailureEvent + OCCURRED_ON + DOCUMENTED_IN.
- `infra/neo4j/seed/05_work_orders.cypher` — 12 WorkOrder + PERFORMED_ON + PERFORMED_BY. WO-1002 and WO-1007 are deliberately "Overdue" — the missed-PM root causes behind FE-001 and FE-004, for RCA Agent demo traversal.
- `infra/neo4j/seed/06_regulatory_clauses.cypher` — 4 RegulatoryClause (2 Factories Act 1948, 2 OISD-STD-132) + APPLIES_TO. The two OISD-STD-132 clauses deliberately apply to PSV-701, the same equipment behind FE-004 — a ready-made Compliance Agent "gap" demo.
- `data/documents/pump_pm_sop.md`, `data/documents/incident_log.md` — synthetic clean-digital documents.
- `data/pnid/sample_pnid.pdf` — real public P&ID course PDF (source in `data/SOURCES.md`).
- `data/SOURCES.md` — provenance tracking for all real vs. synthetic data.

## 2026-07-08 — Phase 1 verified live, complete

**Scanned sample**: user supplied `data/scanned/scnd1.jpeg` (not a PDF as originally planned) — updated `DOC-SCAN-001.source_file` in `03_documents_and_procedures.cypher` and `data/SOURCES.md` to match. Content: handwritten note referencing "Pump P-101" — confirmed P-101 exists in seed data and maps to FE-001 (bearing wear from missed lubrication, WO-1002/WO-1001), so this becomes a second independent RCA-agent demo thread (OCR path) alongside PSV-701/FE-004 (Compliance-agent gap demo).

**TLS chain bug on this machine**: `certifi`'s CA bundle contains the correct roots (SSL.com Root, Certum Trusted Network CA) but OpenSSL 3.5.6 fails to build the chain from Aura's leaf cert (issued by SSL.com RSA SSL subCA, cross-signed via Certum) and misreports it as "self-signed certificate in certificate chain." Windows' native cert store resolves it fine. Fix: added `truststore` (makes Python's `ssl` module use the OS store instead of certifi) with 2 lines in `ingestion/loader/load_seed.py`. This is host-machine-specific, not a code or Aura config issue — if Phase 2's ingestion workers hit the same `SSLCertVerificationError: self-signed certificate in certificate chain` against any Neo4j/Qdrant/API endpoint on this machine, apply the same fix rather than re-diagnosing.

**Aura instance username/database are NOT literally "neo4j"**: this specific Aura instance's official credentials file gives numeric/instance-id values for `NEO4J_USERNAME` and `NEO4J_DATABASE` (matching the instance id in the URI), not the literal string `"neo4j"` I'd assumed as an Aura-wide default. Always pull username/database directly from the instance's own downloaded credentials file, never assume `neo4j`/`neo4j`.

**Live verification** (`a2eb70e2.databases.neo4j.io`): node/relationship counts match seed exactly — Equipment 15, Person 6, FailureEvent 6, WorkOrder 12, Procedure 1, Document 4, RegulatoryClause 4; HAS_PART 3, GOVERNS 2, OCCURRED_ON 6, DOCUMENTED_IN 6, PERFORMED_ON 12, PERFORMED_BY 12, APPLIES_TO 7. Spot-checked P-101→FE-001→{WO-1001,WO-1002} traversal and confirmed em-dash characters in `root_cause` text are stored correctly (U+2014) — an earlier "�" in terminal output was a console codepage display issue, not data corruption.

Phase 1 is done. Moving to Phase 2 (full ingestion pipeline) next.

## 2026-07-08/09 — Phase 2: full ingestion pipeline

**Architecture decisions confirmed before build** (see conversation, not re-litigated below):
- Chunk embeddings deferred to Phase 3. Phase 2 creates `Chunk.text`/`page_ref` only, `embedding` stays null. **Phase 3 must do this as a backfill** (`MATCH (c:Chunk) ... SET c.embedding = ...` over existing Chunks), not a full re-ingestion re-run — the chunks/entities already exist and are correct, only the embedding step is missing.
- MENTIONS is closed-world: only links to `Equipment.tag_id`/`Person.name` already seeded. Never auto-creates new nodes from extraction — unmatched mentions are logged (stdout) and dropped, not persisted anywhere (a deliberate scope cut — see "ground truth validation" below).
- Cloud OCR fallback and P&ID vision both use **Gemini** (not Google Vision, not AWS Textract) — one provider, one key, one SDK (`google-genai`).

**File layout**: `ingestion/pipeline.py` (the Section 8 "convergence step" — owns the one Neo4j driver, one Gemini client, chunking dispatch, entity extraction, fuzzy matching, MERGE loading), `ingestion/parsers/{clean_text,ocr,vision_pnid}.py`, `ingestion/workers/{watcher,tasks}.py`, `ingestion/gemini_util.py` (shared rate-limited Gemini wrapper), `ingestion/cli.py`, `ingestion/ground_truth.json` + `validate_ground_truth.py`.

**Routing**: directory-first for the known seed corpus (`data/documents/`→clean-text, `data/scanned/`→OCR, `data/pnid/`→vision) — this overrides any content heuristic, because `classify_pdf()`'s "text-less → vision" rule doesn't match reality for `sample_pnid.pdf` (it's a text-heavy 42-page course PDF with diagrams embedded on specific pages, not a text-less scan). New files dropped into `data/incoming/` (continuous ingestion) fall back to extension/heuristic dispatch since they have no pre-assigned `Document.type`.

**Chunking**: markdown split on `## ` headers (incident_log.md → one chunk per FailureEvent — validated as the right retrieval granularity); generic PDF per-page via `pdfplumber`, further split on blank-line paragraphs if a page exceeds 1500 chars; OCR image → single chunk; P&ID → one chunk per page with a detected diagram (empty pages skipped), `page_summary` becomes `Chunk.text`.

**Fuzzy matching**: stdlib `difflib` + a targeted regex fix for OCR digit-confusions in the tag's numeric suffix only (`O/o→0`, `I/l→1`, `S/s→5`, `B→8`, e.g. "P-1O1"→"P-101"), exact match first then `difflib.get_close_matches(cutoff=0.8)`. No `rapidfuzz` — correct at 15 Equipment + 6 Person nodes, revisit only if the roster grows materially.

**Entity-extraction prompt bug found via live validation**: Gemini initially misclassified `FE-001`/`WO-1002`-style record ids as "equipment" (they match the same `LETTERS-DIGITS` shape as `PSV-701`). Fixed by explicitly excluding `FE-xxx`/`WO-xxx`/`DOC-xxx`/`PROC-xxx` in the extraction prompt and naming the actual equipment-class prefixes in use. Confirmed fix: incident_log.md went from 6 spurious unmatched "equipment" (the FE- ids) to 0.

**Tesseract not installed on this dev machine** — `pytesseract.TesseractNotFoundError` is caught in `_tesseract_pass()` and treated as confidence 0.0 (i.e., identical to "low confidence"), so it falls straight through to the Gemini fallback rather than crashing. Both branches of the OCR path are still real code, just always exercising the fallback branch here. `TESSERACT_CMD` env var added (commented, in `.env.example`) for machines where the binary exists but isn't on PATH.

**Gemini free-tier quota is a hard daily cap, not just per-minute** — discovered live: `gemini-2.5-flash` free tier allows 5 RPM *and* only 20 RPD (requests per day), confirmed via the API's own error response and Google's rate-limits docs (RPD resets at midnight Pacific, applied per-project not per-key). Added `ingestion/gemini_util.py`'s `throttled_generate()` — a single shared wrapper (13s min spacing between calls + one fixed-delay retry on 429) used by all three Gemini call sites (`pipeline.py`, `ocr.py`, `vision_pnid.py`) so this is enforced in one place. Ran out of the day's quota mid-validation (SOP + incident log alone used 14 calls); switched to a second API key from a different Google account/project to continue (quota is per-project, so a fresh project/account = a fresh 20/day). **Action item before demo day**: enable billing (Tier 1) on whichever account is used for the live demo — free tier's ~20 RPD will not survive a real demo plus RAGAS/agent traffic in Phase 3+.

**P&ID vision extraction**: returns `entities` (→ MENTIONS, closed-world matched) + `connections` + `page_summary` (→ `Chunk.text`) per page. `connections` are logged/printed only, **not written as graph edges** — Section 6's schema has no relationship type for P&ID line connections and inventing one is out of Phase 2 scope. `vision_extract()`/`ingest_file()` accept an optional `pages`/`vision_pages` param to limit which pages are processed — `None` (all pages) is the correct default for real ingestion; used `pages=[3,21]` for dev validation to conserve the 20/day quota. Validated against real diagram pages (found via a zero-quota `pdfplumber` text search for "Figure N:" captions, not guessed) — extraction quality is good (7-45 entities, coherent summaries), but since `sample_pnid.pdf` is a generic training document (see `data/SOURCES.md`), none of its example tags (P-4-3, V-1..V-8, etc.) match Rampur plant equipment — 0 real MENTIONS from this document is the **correct**, expected outcome, not a bug.

**Idempotent dedup**: added `Document.content_hash` (SHA-256), checked in `pipeline.resolve_document()`. Validated live: re-running ingestion on an unchanged `pump_pm_sop.md` correctly resolved to `"unchanged"` and skipped all API calls/chunk regeneration.

**Ground truth validation** (`ingestion/ground_truth.json` + `validate_ground_truth.py`, per PRD §12): 13/13 expected mentions matched, 0 missed, 0 spurious, across all 4 seed documents. Diffs actual graph `MENTIONS` edges against a hand-authored expected set per document — doesn't check the unmatched-log behavior (e.g. `DOC-SCAN-001`'s "R. Sharma" correctly logged as unmatched) since unmatched mentions aren't persisted anywhere by design; that was verified live during the session instead.

**Environment gaps found (not code issues)**: Tesseract OCR binary not installed on this machine (noted above). Docker Desktop is installed but its daemon wasn't running when the watcher/Redis step was reached — needs a manual start before `docker compose -f infra/docker-compose.yml up -d redis` will work.

**RQ's default worker doesn't run on Windows**: `rq worker` forks a "work horse" process per job via `os.fork()`, which doesn't exist on Windows (`AttributeError: module 'os' has no attribute 'fork'` — confirmed live, the watcher/enqueue side worked correctly, only the worker's job-execution crashed). Fix: run with `rq worker aurag-ingest --url <REDIS_URL> --worker-class rq.worker.SimpleWorker` — `SimpleWorker` executes jobs in-process instead of forking (loses per-job timeout enforcement, which doesn't matter for a single-worker hackathon setup). Documented in README's Phase 2 setup section so this doesn't need re-discovering.

**Continuous ingestion validated end-to-end**: started `ingestion/workers/watcher.py` + `rq worker ... --worker-class rq.worker.SimpleWorker`, dropped a test file into `data/incoming/` — watcher enqueued it, worker processed it (new → 1 chunk, 3 mentions linked, 9.3s including one Gemini call). Re-dropped the identical file: resolved to `"unchanged"` in 0.24s, confirming the content-hash dedup (not the 2s in-memory debounce, which had long expired) is what actually prevents duplicate graph writes when the watcher legitimately fires more than once for the same content. Test document/chunk cleaned up from the graph afterward — not part of the permanent seed corpus.

## 2026-07-09 — Phase 2 review: demo-day LLM risk + P&ID connections deferral

**Section 5 deviation: RCA/Compliance/Lessons-Learned sub-agents use Groq, not Gemini.** Section 5's tech table names Gemini for these three agents' reasoning; that's overridden.
**Why**: Gemini's free-tier daily quota (20 RPD, discovered live during Phase 2 — see above) is a per-*query* risk if any sub-agent's live reasoning calls run through it during the demo, unlike Phase 2's ingestion-time Gemini usage (P&ID vision, OCR fallback, entity extraction) which only runs once per document, not once per user question. We are not enabling billing, so this needs a structural fix, not a "remember to upgrade" note. Groq's free tier (verified 2026-07-09 via console.groq.com/docs/rate-limits) gives `llama-3.3-70b-versatile` 1,000 RPD / 30 RPM — headroom that comfortably survives a live demo even with Supervisor routing also on Groq.
**Decision**: Supervisor/routing → `llama-3.1-8b-instant` (14,400 RPD; a classification task doesn't need a 70B model, and it's faster — the original reason Section 5 picked Groq for routing at all). RCA/Compliance/Lessons-Learned reasoning → `llama-3.3-70b-versatile` (best quality on Groq's free tier). Gemini → reserved strictly for P&ID vision parsing at ingestion time, never invoked per-query.
**Trade-off acknowledged**: `llama-3.3-70b-versatile` is a capable open model but generally less nuanced than Gemini 2.5 Flash on complex reasoning. Judged acceptable here because RAGAS scores faithfulness-to-retrieved-context, and a model that stays close to grounded context rather than elaborating past it tends to score as well or better on that axis specifically.
**How to apply**: when building `agents/` in Phase 4, the Supervisor and the three reasoning sub-agents' LLM client config should point at these two Groq models per the split above — not at Gemini, regardless of what Section 5's table literally says.

**P&ID connections (from `vision_pnid.py`'s `PIDConnection` objects) remain logged, not written as graph edges** — confirmed as a deliberate deferral, not a gap to close now.
**Why**: Section 6's schema has no relationship type for P&ID-derived equipment-to-equipment connections (piping/wiring interconnections). `HAS_PART` is semantically wrong for this (containment/composition, not connection/flow) and adding a new relationship type is a schema decision, which — same as every other schema decision this session — should be made deliberately with a known consumer in mind, not added reactively because the extracted data happens to be sitting right there. No sub-agent built so far needs to traverse P&ID connections.
**How to apply**: revisit once an actual agent (most likely RCA, tracing physical interconnection paths) has a concrete need for this data. If/when that happens, add a relationship type (e.g. `CONNECTED_TO`) to `infra/neo4j/schema.cypher` and a loading step in `pipeline.py`'s vision-route branch — the `PIDConnection.from_tag`/`to_tag`/`line_type` fields already captured are sufficient, no re-extraction needed.

**Tesseract OCR engine properly installed** (`winget install UB-Mannheim.TesseractOCR`, v5.4.0.20240606) — previously the OCR path silently always fell through to Gemini because the binary was absent, which defeated the point of having a free local-first OCR step at all. `.env`'s `TESSERACT_CMD` uncommented and pointed at the real install path.

**Real bug found while wiring this up**: `ocr.py` was reading `TESSERACT_CMD` from `os.environ` at *module import time*, but every entry point (`pipeline.py`, `ocr.py`'s own `__main__`) calls `load_dotenv()` in its module body *after* its `import` statements — meaning the env var was never actually set yet when `ocr.py`'s top-level code ran, regardless of entry point. This would have silently no-op'd even with `TESSERACT_CMD` correctly set in `.env`. Fixed by moving the read inside `_tesseract_pass()` (call time, not import time) — general lesson: don't read `os.environ` at module import time in a codebase using `load_dotenv()`, since import order almost never guarantees `.env` is loaded first.

**Verified against the real scanned sample post-fix**: Tesseract now genuinely runs and returns mean confidence 59.2 (previously always 0.0/binary-not-found) with garbage text (`'ie Y ote = (2 � b'`) — an honest failure on cursive handwriting, not a stub. Confidence sits just under the 60 threshold, so it still correctly falls back to Gemini for the real transcription. This is exactly the intended behavior: the free local path genuinely runs and is only bypassed when actually warranted, not silently skipped.

## 2026-07-09 — Phase 3: hybrid retrieval layer

**Reranker: local cross-encoder, not Cohere Rerank (Section 5's stated tech).** `retrieval/rerank.py` uses `sentence_transformers.CrossEncoder("cross-encoder/ms-marco-MiniLM-L-6-v2")` instead.
**Why**: caught during plan review — Cohere Rerank is a per-query external API call (network dependency + shared quota pool), the exact risk category already eliminated three times over this project: Groq over Gemini for sub-agent reasoning (see Phase 2 review above), local `sentence-transformers` embeddings over Cohere embed, and local Docker Qdrant over Qdrant Cloud. Leaving Cohere Rerank as the sole remaining external per-query dependency would have broken that consistency for no benefit — same library already installed for embeddings, no new dependency, no new API key, no quota risk at demo time. Validated as working well (5/5 hand-authored ground-truth queries pass; see below).
**How to apply**: Phase 3 requires **zero external API keys**. `COHERE_API_KEY` never entered `.env` — only `QDRANT_URL` was added. If a future phase reconsiders a hosted reranker, re-derive this decision rather than assuming it still holds.

**Embedding model: `sentence-transformers` `all-MiniLM-L6-v2`, 384-dim, local** — decided before build (see plan), same reasoning as above. `retrieval/embeddings.py` is a lazy-init singleton mirroring `pipeline.py`'s `get_driver()`/`get_gemini_client()` pattern.

**Two vector stores, not one standing in for the other**: Neo4j native vector index (`CREATE VECTOR INDEX`, confirmed available on AuraDB Free — only the extra memory-tuning config is gated to Business Critical+) and Qdrant (local Docker, no persistent volume) both hold the same 384-dim embeddings and are both queried in `hybrid.retrieve()`. Section 4's architecture diagram calls for both explicitly; redundant but cheap at this corpus size, and gives two independent recall paths that can disagree (useful for the GraphRAG-comparison differentiator later).
**`db.index.vector.queryNodes` deprecation warning**: this Neo4j 5.x build warns it's "replaced by 'SEARCH'" but still executes correctly. Left as-is — not worth chasing a moving-target Cypher syntax mid-hackathon; documented inline in `hybrid.py`.

**BM25 rebuilt fresh from Neo4j on every call, not cached** (`retrieval/bm25_index.py`). Corpus is ~17-50 Chunks; rebuild is single-digit ms. Add persistence/incremental updates only if the corpus grows into the thousands.

**Graph traversal query-time entity extraction is not an LLM call** — `retrieval/graph_traversal.py` reuses `ingestion.pipeline`'s closed-world fuzzy matchers (`load_known_entities`/`match_equipment`/`match_person`) directly against the raw query text (whitespace tokens for equipment tags, adjacent-word bigrams for "First Last" person names), same reasoning as the Groq-for-reasoning/local-embeddings decisions: retrieval should never depend on a live external call per query. Anchors on matched Equipment/Person nodes, pulls both directly-linked Chunks and synthesized-text passages from structured records (FailureEvent/WorkOrder/RegulatoryClause/Procedure) via their existing relationships.

**Fusion**: `hybrid.retrieve()` unions candidates from all four sources into a `dict[key, text]` (last-write-wins on duplicate keys — safe, since a duplicate key always carries the same underlying text), then reranks the full candidate set with the local cross-encoder, returns top-k. Plain `(key, text)`/`(key, text, score)` tuples throughout — no `Passage` dataclass; 2-3 fields didn't earn one, and it matches the tuple style `bm25_index.py`/`qdrant_store.py` already used.

**mem0 stays out of scope for this phase** — Section 12's Phase 3 row lists only Neo4j+Qdrant+BM25+rerank; mem0 is cross-session agent memory, a Phase 4/5 concern once sub-agents exist to have memory of.

**Continuous-ingestion freshness**: `retrieval/index_chunks.py`'s `index_chunks(session, chunk_ids=None)` embeds + writes `Chunk.embedding` in Neo4j + upserts into Qdrant. `chunk_ids=None` = full backfill (used once for the pre-existing 17 Chunks); a specific id list = incremental. Wired into `ingestion/pipeline.py`'s `ingest_file()` — called once at the end of every successful "new"/"changed" ingest (never reached for "unchanged", which returns early), so the watcher/RQ continuous path keeps embeddings current automatically, not just at the one-time backfill. One function, two call sites, no duplicated indexing logic.

**Validation** (PRD §14: "test each retrieval path independently before combining"): each of the 4 sources has its own `__main__`/standalone self-check (`qdrant_store.py`, `bm25_index.py`, `graph_traversal.py`, `_selfcheck_neo4j_vector.py` — see below) confirming sane output in true isolation (no fusion, no rerank) before `hybrid.py` combines them. `retrieval/ground_truth.json` + `validate_retrieval.py` (mirrors `ingestion/validate_ground_truth.py`'s report format) hand-authors 5 queries anchored on known seed facts (P-101/FE-001 bearing failure, PSV-701/FE-004/OISD-STD-132-10.2ii compliance gap, P-102 seal replacement, HX-401 fouling, C-201 trip) — unlike the ingestion ground truth's exact-set diff, this checks "at least one expected key in the top-5" rather than exact recall, since the graph has many legitimately-related records per query and exact-set matching isn't the right bar for fused retrieval. Result: 5/5 queries matched.

**Q1 ground truth diff, spelled out precisely** (this was a scope correction, not a loosened bar): `expected_any` went from `["FE-001", "WO-1001", "WO-1002"]` to `["FE-001", "WO-1001", "WO-1002", "DOC-LOG-001-C002"]` — **one key added, zero removed**. The first validation run flagged Q1 as a mismatch because the fused+reranked top hit was `DOC-LOG-001-C002`, not one of the three original structured-record ids. Before touching the ground truth, that chunk's actual stored text was inspected directly in Neo4j: it opens `"FE-001 — P-101 Drive-End Bearing Failure (2025-03-14)... **Symptom:** Excessive vibration..."` — i.e. it *is* the FE-001 event, just expressed as prose narrative (from `incident_log.md`) rather than as the structured graph node. It is not a different fact, not a tangential result, and not a lower-quality answer — it's the same underlying root cause the original three keys were already pointing at, in a second valid representation the original ground truth simply didn't anticipate. Because all three original keys are still required (any one of them still passes the query), this is strictly additive: a query that used to only accept the structured answer now also accepts the equally-correct prose answer. Nothing about what counts as "wrong" was relaxed.

**Demo material (PRD §9/§10 — "hybrid beats single-method retrieval" differentiator, real measured evidence, not a borrowed example)**: query `"Why did P-101 fail in March 2025?"`. Neo4j vector search alone (isolated, see below) misses the ideal chunk `DOC-LOG-001-C002` entirely — top-5 is four tangential hits (P-102 seal leak, SOP boilerplate ×2, PSV-701, a log header). BM25 alone finds `DOC-LOG-001-C002` directly (keyword match on "bearing lubrication"-type terms). Graph traversal alone finds `FE-001` directly (entity anchor on the `P-101` tag). Qdrant dense search performs comparably to Neo4j vector search (same embeddings, same near-miss). The fused + cross-encoder-reranked result promotes `DOC-LOG-001-C002` to **top-1** — correct only because BM25 and graph traversal both surfaced it and reranking recognized it as the best-matching text once it was in the candidate pool. **Demo script**: run each of the four source self-checks live against this exact query side-by-side with `python -m retrieval.hybrid`, show vector-only missing it, then show fusion landing it at #1 — concrete, reproducible, no cherry-picked or synthetic example.

**Isolation-testing gap found and closed**: on review, Neo4j-native-vector-only had never had its own standalone self-check — it was only ever exercised as one contributor inside the combined `hybrid.py` fusion test, which doesn't satisfy §14's "independently before combining" requirement on its own. Wrote `retrieval/_selfcheck_neo4j_vector.py` (bare `db.index.vector.queryNodes` call, no BM25/Qdrant/graph traversal in the loop) and ran it fresh against the Q1 query. Result: in true isolation, Neo4j vector search does **not** surface `DOC-LOG-001-C002` in its top-5 — it returns four tangential chunks instead (FE-006/P-102 seal leak, generic SOP procedure text ×2, FE-004/PSV-701, a log header). Meanwhile BM25 and graph traversal, each independently self-checked, *do* find the right answer directly (BM25 surfaces `DOC-LOG-001-C002` for "bearing lubrication"; graph traversal anchors on the `P-101` tag and pulls `FE-001` directly). This is a useful negative result, not a failure: it's direct evidence that no single retrieval source is sufficient on its own, and that the fusion + cross-encoder rerank step is doing real work — promoting the correct chunk to top-1 in the combined result specifically because BM25 and graph traversal both surface it even though vector search alone misses it. Kept `_selfcheck_neo4j_vector.py` as a permanent addition to the retrieval self-check suite (renamed without the leading underscore) rather than deleting it post-run, since it directly covers the one source that previously had no standalone regression check.

## 2026-07-09 — Phase 4: Copilot / RCA / Compliance / Lessons Learned sub-agents

**Directory layout corrected: flat modules, not empty package dirs.** Phase 1 scaffolded `agents/compliance/`, `agents/copilot/`, `agents/lessons_learned/`, `agents/rca/` as empty subdirectories. Removed all four, replaced with flat `agents/{copilot,rca,compliance,lessons_learned}.py`.
**Why**: every self-checked module elsewhere in this repo (`ingestion/`, `retrieval/`) is a flat file runnable via `python -m package.module`, relying on the module's own `if __name__ == "__main__":` guard. `-m` on a package directory instead executes `pkg/__main__.py` (which none of these had) — the empty dirs would have silently broken the established self-check convention the moment anyone tried to run one directly.
**How to apply**: if a future phase genuinely needs per-agent multi-file structure (e.g. a Supervisor with its own prompt/tools/tests submodules), promote that one agent back to a package deliberately, don't do it preemptively for all four.

**Copilot extended onto the Phase 2 review's Groq split, which only named three agents.** The 2026-07-09 Phase 2 review (above) decided "RCA/Compliance/Lessons-Learned reasoning → `llama-3.3-70b-versatile`" without mentioning Copilot.
**Why**: Copilot is a Q&A/reasoning task over retrieved context, same shape as the other three — not a routing/classification task like the Supervisor (`llama-3.1-8b-instant`). No reason to single it out for a different model, and Gemini remains off the table for the same per-query-quota risk reasoning as the original review.
**Decision**: all four Phase 4 agents use `llama-3.3-70b-versatile`. Logged here explicitly rather than assumed silently, per this project's standing rule on unresolved PRD/decision gaps.

**`agents/llm.py`: retry-only Groq client, no mandatory pre-call throttle (contrast `ingestion/gemini_util.py`'s 13s `_MIN_INTERVAL`).**
**Why**: Groq's free tier (1,000 RPD/30 RPM on `llama-3.3-70b-versatile`, 14,400 RPD on `llama-3.1-8b-instant`) is far above Gemini's 20 RPD hard cap that caused real quota exhaustion in Phase 2. A fixed pre-call delay would only slow down every agent call for a risk that doesn't apply here. `ask_json()` mirrors the shared-client-singleton shape (`_client`/`get_client()`, same pattern as `pipeline.py`'s `get_driver()`/`get_gemini_client()` and `embeddings.py`'s `get_model()`) and does one retry on `RateLimitError`, matching `gemini_util.py`'s one-retry shape without the throttle.

**Structured JSON citations, not parsed prose.** Every agent asks Groq for `{"answer": string, "citations": [string, ...]}` via `response_format={"type": "json_object"}`, reusing the same "LLM structured JSON output" pattern PRD §5 already endorses and `ingestion/pipeline.py`'s Gemini entity extraction already proved out. If the model's JSON omits `citations`, `ask_json()` falls back to the full set of context keys passed in — citations from an agent are never silently empty just because the model forgot the field.
**Why not fuzzy-match free text**: `agents/validate_agents.py` needs a deterministic pass/fail signal per query (see below); parsing citations out of prose would make validation itself unreliable.

**`agents/util.py`'s `classify_key()`/`graph_paths()` build the answer-trail data now, rendering deferred.** Every agent's `citations` list gets typed into `graph_paths` (`{"type": "FailureEvent", "id": "FE-001"}` etc.) via id-prefix convention confirmed against `infra/neo4j/seed/*.cypher` (`FE-`→FailureEvent, `WO-`→WorkOrder, `PROC-`→Procedure, `FACT`/`OISD-`→RegulatoryClause, `DOC-`→Chunk). Equipment/Person anchors (RCA/Compliance, matched via `extract_query_entities`) are typed directly by the agent, not run through the prefix classifier — equipment tags (`P-101`, `PSV-701`) have no reliable common prefix.
**Why now**: PRD §9 differentiator #2 is the Question → Chunk → Equipment → Failure → Procedure answer-trail visualization. The frontend doesn't render it until a later phase, but the typed data needs to exist at the agent layer now rather than being retrofitted later.

**RCA and Compliance both reuse `retrieval.graph_traversal.traverse(session, query, top_k=None)` — no new Cypher for either.** `top_k=None` (not the retrieval-layer's default top-5) to get the full entity-anchored record set, since these agents reason over structured records (FailureEvent/WorkOrder/RegulatoryClause/Procedure), not a ranked top-k like Copilot's fused retrieval.
**Why Compliance has no new "clause satisfied by work order" Cypher relationship**: the seed graph has no such edge, and inventing one now would be a schema decision made reactively for one agent rather than deliberately (same principle already applied to P&ID connections in the Phase 2 review). Gap detection (e.g. an Overdue work order against a calibration clause) is instead a reasoning task for the LLM over the structured context `traverse()` already returns.
**Both agents share the same "no entity matched" boundary handling**: if `extract_query_entities` finds no equipment/person, return a clear message and skip the LLM call entirely — nothing to reason over yet (no Supervisor exists yet to catch/redirect this case), calling the LLM anyway would only invite hallucination over empty context.

**Lessons Learned is whole-corpus by design, no per-query entity extraction.** One new small Cypher (`MATCH (fe:FailureEvent)-[:OCCURRED_ON]->(e:Equipment) OPTIONAL MATCH (wo:WorkOrder)-[:PERFORMED_ON]->(e) ...`) pulls every FailureEvent + related WorkOrders in one pass.
**Why a new query is justified here** (unlike RCA/Compliance): nothing existing does a full-corpus scan — `graph_traversal.traverse()` is entity-anchored by construction, the wrong shape for "find patterns across incidents." Seed dataset is 4 FailureEvents, no scale concern justifying pagination/sampling.

**Validation**: `agents/ground_truth.json` (2 queries × 4 agents, 8 total) + `agents/validate_agents.py`, mirroring `retrieval/validate_retrieval.py`'s `expected_any`-intersection report format (not `ingestion/validate_ground_truth.py`'s exact-set diff) — but checked against each agent's structured `citations` list rather than a retrieval `key` tuple, since citations are deterministic and prose isn't. Anchored on the same known seed facts already used in Phase 3's ground truth (P-101/FE-001/WO-1002 bearing failure; PSV-701/FE-004/WO-1007/OISD-STD-132-10.2ii calibration gap) plus C-201/FE-002/WO-1003 (fouled intercooler trip) for cross-equipment coverage in the Lessons Learned queries.

**No shared CLI this phase, deliberately.** Each agent is only reachable via its own `__main__` self-check or a direct `answer(session, query)` call — no unified entry point routing a query to the right agent. That's Phase 5's job (Supervisor/LangGraph), once there's an actual router to build a CLI on top of; building one now would be scaffolding for a routing layer that doesn't exist yet.

**Verified live, all green.** `GROQ_API_KEY` supplied, all six self-checks (`agents.llm`, `agents.copilot`, `agents.rca`, `agents.compliance`, `agents.lessons_learned`) plus `agents.validate_agents` ran clean — **8/8 ground-truth queries matched**. Two real bugs surfaced during this pass, both fixed:

**Bug 1 — `agents/rca.py` self-check was over-specified.** First run failed: `assert any(c.startswith("WO-") ...)` tripped even though the answer was correct — the model cited `PROC-001` (the governing fix procedure) instead of a `WO-` key. Confirmed via a standalone `graph_traversal.traverse()` inspection that both `WO-1001`/`WO-1002` genuinely were in context; the model just chose a different, equally-valid citation.
**Why**: same category as the Q1 ground-truth diff above — the bar was tighter than what's actually guaranteed. The model legitimately picks which retrieved context to cite; a specific `WO-` key was never a hard guarantee, only an `FE-` anchor is.
**Fix**: relaxed the assertion to only require an `FE-` citation, with a `ponytail:` comment on why. Confirmed non-deterministic across runs — re-running the same query later produced yet another different (still valid) citation set.

**Bug 2 — citation echoing in `agents/llm.py::ask_json()` (the real find).** `agents.compliance`'s self-check "passed" on a loose substring check but `result["citations"]` actually contained full passage sentences (e.g. `"WO-1007 (Preventive, Overdue): Scheduled annual PSV calibration"`) instead of bare keys (`"WO-1007"`). This would have silently broken `validate_agents.py`'s exact-set `got & expected` matching and corrupted the `id` field inside `graph_paths()`'s typed answer-trail output.
**Root cause**: only Copilot's system prompt told the model to cite the bracketed key, not the passage text — RCA/Compliance/Lessons Learned's prompts never said so.
**Fix (root cause, not per-agent patch)**: fixed once in the shared `ask_json()` choke-point every agent routes through, not in four separate prompt strings. Added a shared `_CITATION_RULE` string appended to every system prompt, **plus** a hard whitelist filter (`citations = [c for c in (data.get("citations") or []) if c in context_keys]`) so citations are guaranteed valid keys regardless of prompt compliance — defense in depth, since an LLM's JSON-mode field content isn't guaranteed to match what was asked for even when the JSON syntax itself is valid.
**How to apply**: any future agent added to this repo automatically inherits this guarantee for free by calling `ask_json()` — no per-agent citation-format code needed. Re-ran `copilot`/`rca`/`compliance` after the fix to confirm no regressions.

**Bug 2 follow-up — verbatim-vs-paraphrase gap in the narrative `answer` text, closed with a per-agent split, not one blanket rule.** Bug 2's fix only covered the `citations` field format; it said nothing about whether the prose `answer` paraphrases or quotes source passages verbatim, which is a distinct answer-quality concern. Reviewed all four self-check outputs and found no verbatim passage leakage in `answer` at the time, but no prompt instructed against it either — an unaddressed gap, not yet a proven bug.
**Fix, split by agent, not uniform**: Copilot, RCA, and Lessons Learned each got one added sentence in their `_SYSTEM` prompt — *"State findings in your own words — don't copy sentences directly from retrieved context."* Compliance got the opposite instruction: quote `RegulatoryClause` text verbatim when citing it (e.g. `"OISD-STD-132 clause 10.2ii states: '...'"`), keeping only the surrounding gap-analysis reasoning in the model's own words.
**Why Compliance is the deliberate exception**: paraphrasing a regulatory clause's exact wording risks silently changing its legal/technical meaning — the audit-ready evidence text this agent produces is only trustworthy if the clause text itself is reproduced exactly, not summarized. This isn't a relaxation of the paraphrase rule; it's recognizing regulatory clause text is source-of-truth material that must be preserved verbatim, the same way a legal quote or a spec number would be, while the agent's own reasoning about it is still free text.
**Verified**: re-ran all four self-checks plus `agents.validate_agents` after the prompt changes (Qdrant container had also restarted since the last run — re-backfilled via `python -m retrieval.index_chunks` first, unrelated to this change). Compliance's self-check now visibly quotes the clause verbatim (`"OISD-STD-132 clause 10.2ii states: 'The Testing and Maintenance History of the Safety Relief Valve must be provided to the in-house testing team prior to testing or calibration'"`) while its gap explanation around it stays in prose. **8/8 ground-truth queries still matched — no regressions.**

## 2026-07-10 — Phase 5: Supervisor / LangGraph routing

Built `agents/supervisor.py` as an actual `langgraph.graph.StateGraph` (installed `langgraph==1.2.9`), not a hand-rolled dispatcher — PRD §5 names LangGraph explicitly as the orchestration choice, and §12's Phase 5 milestone literally says "Supervisor/LangGraph routing," so this was treated as a stated requirement, not optional scope. One `supervisor` node calls `agents.llm.classify_intent()` (new, on `llama-3.1-8b-instant` per the Phase 2 review's routing-model decision — factored the Groq call/retry mechanics into a shared `_call_groq_json()` so `ask_json()` and `classify_intent()` don't duplicate client/retry logic); conditional edges route to whichever of the four agent nodes the classification named, or fall back to `copilot` below a confidence floor (`_CONFIDENCE_FLOOR = 0.6`, a ponytail-flagged fixed threshold, not tuned against a large query set — revisit if real misroutes show up). This directly implements PRD §14's named risk mitigation: "build an explicit fallback path (generic hybrid retrieval + Copilot) for low-confidence routing." `classify_intent()` also never trusts the model's `intent` field at face value — an unrecognized label normalizes to `{"intent": "copilot", "confidence": 0.0}`, same defensive pattern as Bug 2's citation whitelist, and it composes for free with the confidence floor (0.0 always fails it).

**Real bug found by the fallback-edge test, fixed at the root.** The plan called for a deterministic test: monkeypatch `classify_intent` to force `{"intent": "rca", "confidence": 0.3}`, invoke the graph, assert the result's `intent` is `"copilot"`. First run failed — not because the fallback routing was broken, but because `state["intent"]` was only ever set by the `supervisor` node (the classifier's *raw pre-fallback guess*) and never touched by whichever agent node actually executed. So even when the graph correctly routed to `copilot` under the hood, `result["intent"]` still read `"rca"` — the classifier's original guess, not the agent that actually produced `agent_response`. This isn't just a test-proof problem: any real downstream consumer (RAGAS-by-intent scoring, the answer-trail UI) would have seen `intent: rca` on an answer `copilot` actually generated, which is misleading, not just cosmetic.
**First fix was incomplete — it traded one telemetry gap for a smaller one.** The initial fix had each agent node stamp `result["intent"] = name`, so `state["intent"]` always reflected whichever agent actually ran. That made the fallback-edge test pass, but on review it silently discards the classifier's *original* guess whenever a fallback fires — a fallback case would show `intent: copilot` with no trace that the classifier actually said `rca` first. That's still a real debugging blind spot for tuning `_CONFIDENCE_FLOOR` later: you'd know the confidence score but not what it was a confidence *of*.
**Corrected fix**: added a second field, `routed_agent`, to `AgentState` (`agents/state.py`). `intent` now stays exactly what the Phase 1 pre-decision meant it to be — the classifier's raw guess, untouched by which node executes. `routed_agent` (new) is what each agent node stamps with its own name — the two fields are identical in the normal (non-fallback) case and diverge only when the confidence floor overrides the classifier's guess, which is exactly the case worth being able to see. `validate_supervisor.py`'s fallback-edge test now asserts both: `intent == "rca"` (the mocked guess survived) **and** `routed_agent == "copilot"` (the fallback fired) — a strictly stronger deterministic proof than checking either field alone. Re-ran the full suite (`agents.supervisor`, `agents.validate_supervisor`, `agents.validate_agents`) after this correction — all still pass, 8/8 on both ground-truth checks.

**Real domain-overlap found via ground-truth reuse, not a routing bug — extended `expected_any`'s tolerance pattern to intents.** Reusing Phase 4's `agents/ground_truth.json` for routing validation (its outer keys are the expected intent) surfaced that *both* of Copilot's own ground-truth queries route to a more specific agent instead: `CQ1` ("Why did P-101 fail in March 2025?") → `rca` (0.92 confidence), and `CQ2` ("What is the relief valve calibration requirement for PSV-701?") → `compliance` (0.99 confidence). Investigated both before touching anything: `CQ1`'s phrasing is textually identical in shape to RCA's own PRD definition ("why did equipment X fail") and its own ground truth (`RQ1`: "What caused the P-101 failure?"); `CQ2` asks about a regulatory calibration requirement, which is closer to Compliance's domain than a generic lookup. In both cases the routed agent's answer still hit the correct citations (`CQ1`→rca cited `FE-001`/`WO-1002`/`DOC-LOG-001-C002`, the exact Phase 3 demo chunk; `CQ2`→compliance cited `OISD-STD-132-10.2ii`/`WO-1007`). This isn't a fixable-via-better-prompt problem — Copilot's "general Q&A" domain necessarily overlaps with every specialized agent's domain, because a specific, well-formed question is *also* a valid general question. Narrowing Copilot's prompt to exclude root-cause/compliance-shaped questions would just break its own stated purpose.
**Fix**: added an `acceptable_intents` field to `agents/ground_truth.json`, only on the two queries where the overlap is real (`CQ1`: `["copilot", "rca"]`; `CQ2`: `["copilot", "compliance"]`) — the same `expected_any`-style tolerance already used for citations (accept any legitimate answer, don't force one rigid "correct" answer where several are defensible), applied to intents instead. `validate_supervisor.py`'s `_check_ground_truth` reads `case.get("acceptable_intents", [expected_intent])` so all other six queries keep their original strict single-intent check unchanged.
**Result**: `8/8` clear-intent queries routed+answered correctly, ambiguous/mixed-intent queries (`agents/ambiguous_queries.json`) all handled gracefully (non-empty answers, no crashes), and the fallback-edge test deterministically confirms the confidence-floor branch fires. Re-ran `agents.validate_agents` afterward to confirm the `agents/llm.py` refactor (`_call_groq_json` extraction) didn't regress Phase 4's four direct-call self-checks — still `8/8`.

**No FastAPI/CLI wiring this phase, deliberately** — same deferral logic as Phase 4's "no shared CLI." `agents.supervisor.answer(session, query) -> dict` is directly callable and independently tested; wiring it into a `backend/` endpoint is Phase 8 (frontend fully connected) territory, once there's a frontend consuming it. `backend/` remains empty scaffolding.

## 2026-07-10 — Phase 6: RAGAS evaluation layer

**Judge-model decision, confirmed with the user rather than defaulted**: RAGAS's judge LLM = Groq `llama-3.3-70b-versatile` (the same model already used by the four reasoning agents), embeddings = the existing local `sentence-transformers` model (`retrieval/embeddings.py`) via a hand-written ~15-line adapter. No new API key, no per-query Gemini use (already ruled out in the Phase 2 review).
**PRD §13 stale-checkbox note**: "RAGAS thresholds" is still shown as an open `[ ]` item in PRD.md, but it was actually resolved back in the Phase 1 NOTES.md entry (≥0.7 pass, 0.5–0.7 amber, <0.5 red; faithfulness <0.7 also sets an inline-banner flag). This is a doc-sync gap in PRD.md's own text, not a real unresolved decision — flagged here rather than re-litigated.

**Dependency pinning was a real fight, not a guess — `evaluation/requirements.txt` pins `ragas==0.4.3`, `langchain-groq==1.1.3`, `langchain-community==0.4.0`, `langchain-core==1.4.9`.**
**Why each pin is load-bearing**: `ragas==0.4.3` eagerly imports `langchain_community.chat_models.vertexai.ChatVertexAI` at package-init time (a legacy multi-provider auto-detection artifact) even though this repo never touches Vertex AI. That submodule was removed from `langchain-community` as of `0.4.1+` (part of its ongoing sunset), so a fresh `pip install ragas langchain-groq` resolves to the latest `langchain-community` and `ragas` itself fails to import at all — not a bug in this repo's code, a real packaging gap in `ragas==0.4.3` against currently-latest `langchain-community`. `langchain-community==0.4.0` is the last patch that still has the submodule, confirmed by directly reading its installed source, and it's still compatible with the `langchain-core>=1.4.7` that `langgraph==1.2.9` (Phase 5) needs.
**A costly detour, logged so it isn't repeated**: before landing on this pin, `ragas` was downgraded to `0.2.15` (an older, more familiar API) on the assumption the 0.4.x line was just "too new/unstable" — this was wrong and premature; it silently pulled `langchain-core` back to `0.3.86`, breaking `agents.supervisor` (`langgraph` needs `>=1.4.7`) entirely. The actual fix was staying on `ragas==0.4.3` and pinning `langchain-community` precisely instead. **How to apply**: when a fresh top-level dependency breaks on import, verify the *specific* import failure before downgrading the whole package — the first instinct (retreat to an older version) made things worse here.
**Side effects on the shared venv, caught and reverted**: the same `pip install` chain downgraded `groq` (the raw SDK `agents/llm.py` uses directly) from `1.5.0` to `0.37.1` as a transitive dependency of `langchain-groq`, and separately bumped `openai` from `2.24.0` to `2.45.0` — the latter is a pin the harness itself (`hermes-agent`) depends on, flagged by pip's own resolver warning. Re-ran `agents.llm`'s self-check immediately after the `groq` downgrade to confirm no behavior change (passed — the chat-completions call shape is stable across those versions), and explicitly reinstalled `openai==2.24.0` to restore harness compatibility rather than leaving a shared-environment side effect unaddressed.

**`bypass_n=True` required on `LangchainLLMWrapper`** — first live scoring attempt failed with `groq.BadRequestError: 'n': number must be at most 1`. RAGAS's `Faithfulness` metric requests multiple statement candidates via `n>1` in a single call by default (fine for OpenAI, rejected outright by Groq's API). `LangchainLLMWrapper`'s constructor has exactly this escape hatch — with it set, the wrapper issues N sequential `n=1` calls instead of one `n=N` call.

**State schema split explicitly, not silently overloaded** (`agents/state.py`): `ragas_scores` stays `dict[str, float]`, numeric-only, populated only when scoring succeeds. Two new fields carry everything else: `ragas_status` (`"scored" | "skipped_no_context" | "error"`) and `low_faithfulness: bool`. A bare `{}` for "not scored" was rejected during planning because it can't distinguish "nothing to score" from "scoring broke" from "here are real numbers" — `ragas_status` makes that explicit for any downstream consumer (RAGAS-by-intent dashboards, the answer-trail UI).

**`score` node in `agents/supervisor.py`: one shared node, fail-open, dependency-lazy.** Appended after each of the four agent nodes, before `END`. Skips scoring entirely (no LLM call) when `retrieved_context` is empty — RCA/Compliance's existing "no entity matched" short-circuit returns a canned message with nothing to score. Otherwise it lazily imports `evaluation.score` *inside* the node function (not at module top-level) and wraps the whole scoring call in `try/except Exception`, so a missing/broken `ragas`/`langchain-groq` install, a Groq API failure, or any internal RAGAS error degrades to `ragas_status: "error"` and an untouched, already-produced answer — never a crashed graph. Verified explicitly via `unittest.mock.patch("evaluation.score.score_answer", side_effect=RuntimeError(...))`: the graph still returned a full answer with `ragas_status == "error"`, at zero Groq cost since the mock never calls out.

**A second `agents/llm.py` gap found live, hardened with explicit approval (core shared file).** During this phase's own regression testing, `rca.answer()` crashed uncaught with `groq.BadRequestError: json_validate_failed` — the model generated syntactically invalid JSON (an unterminated `"answer"` string), which Groq's own JSON-mode validation rejected with a 400 before it ever reached this repo's `json.loads()`. `_call_groq_json()` previously only retried on `RateLimitError`; a `BadRequestError` propagated straight through and crashed the whole Supervisor call. Presented to the user as a second distinct gap in the same function (the first being the TPD-retry-window issue below) — approved and fixed: one retry on `BadRequestError` too, same fixed-retry shape as the existing `RateLimitError` handling. A persistent bad request just fails identically on retry and raises, so this can't loop.

**Groq's `llama-3.3-70b-versatile` daily quota (100k TPD, free tier) is a real, not theoretical, constraint once RAGAS is in the loop — required three separate account swaps to complete this phase's verification.** RAGAS makes several judge LLM calls per single query (faithfulness statement generation + verification, context-precision judging, answer-relevancy question generation), all against the same model and the same shared daily pool as routing + all four agents' own reasoning calls. A single `evaluation.validate_ragas` run (8 queries) came close to fully exhausting a fresh account by itself; running it back-to-back with `agents.validate_supervisor` (which now also scores every query, since scoring lives in the graph) exhausted three different accounts in sequence during this session's testing. Confirmed the hard way that **Groq's quota is scoped per organization, not per key** — swapping to a second key generated on the *same* account did not free up any quota; only a genuinely different account (different sign-up) did. `agents/llm.py`'s existing one-fixed-5-second-retry can't help against a "try again in N minutes" daily-quota error — that's an inherent free-tier ceiling, not a retry-logic bug, and is now called out explicitly in the Phase 6 README section and the pre-demo checklist (use a fresh, unburned key for the actual demo, not whatever key soaked up rehearsal traffic).

**Baseline scores established (PRD §12 Phase 6's actual deliverable), recorded here as the durable artifact, not left in stdout only.** From `evaluation.validate_ragas`'s first fully-successful run (7 of 8 ground-truth queries scored; the 8th — `lessons_learned/LQ2` — hit the Groq quota wall and failed open cleanly, `ragas_status: "error"`, answer still returned):

| Query | Faithfulness | Context Precision | Answer Relevancy |
|---|---|---|---|
| copilot/CQ1 | 1.00 (pass) | 0.29 (red) | 0.72 (pass) |
| copilot/CQ2 | 1.00 (pass) | 0.92 (pass) | 1.00 (pass) |
| rca/RQ1 | 0.89 (pass) | 0.87 (pass) | 0.72 (pass) |
| rca/RQ2 | 1.00 (pass) | 0.92 (pass) | 0.91 (pass) |
| compliance/CoQ1 | 1.00 (pass) | 0.70 (amber) | 1.00 (pass) |
| compliance/CoQ2 | 0.29 (red) | 0.33 (red) | 0.89 (pass) |
| lessons_learned/LQ1 | 0.92 (pass) | 0.57 (amber) | 0.90 (pass) |

**Averages (7 scored queries): faithfulness 0.870 (pass), context_precision 0.657 (amber), answer_relevancy 0.879 (pass).**
**Reading the baseline honestly, not just declaring green**: context precision is the weakest metric overall (2 of 7 red, 2 amber) — Copilot's hybrid retrieval and Compliance's `CoQ2` in particular are pulling in a meaningful amount of context the answer doesn't end up using. `compliance/CoQ2`'s faithfulness (0.29, red) is the single most concerning individual score — PRD §14 explicitly frames a low faithfulness score as a build blocker, not a footnote, so this is flagged as a genuine candidate for prompt/retrieval iteration in a follow-up pass, not swept under "baseline established and done." Per PRD §12's own wording ("iterate on retrieval/prompting until scores stabilize"), that iteration is deliberately treated as separate follow-up work, not bundled into this phase's sign-off.

**Final regression proof**: `agents.validate_supervisor` — 8/8 clear-intent queries routed+answered correctly (Phase 5's routing/citation logic unregressed by this phase's additive-only state/graph changes). `agents.validate_agents` — 8/8 (Phase 4's direct-call agents, which never route through the Supervisor's new `score` node at all, confirmed unaffected).

**Phase 6 sign-off — closeout framing, stated precisely so it isn't misread later.**
1. **"7/8 scored" is 7 successfully scored plus 1 caught by the fail-open mechanism, not a content-driven skip.** The 8th query (`lessons_learned/LQ2`) never hit `ragas_status: "skipped_no_context"` (the genuinely intentional skip path, for when there's no retrieved context to score — none of these 8 ground-truth queries triggered it). It hit `ragas_status: "error"`, meaning the fail-open *mechanism* worked exactly as designed (Groq's quota exhaustion was caught, the answer was still returned, nothing crashed), but the specific miss was an external operational failure (daily token quota), not a principled "this one doesn't need scoring" decision. Both things are true and distinct: the graceful-degradation behavior is by design; which query it happened to catch that day was not.
2. **`compliance/CoQ2`'s faithfulness score (0.29, red) is not resolved by this phase and must not be described as such.** This is precisely the kind of signal PRD §8a/§14 built RAGAS to surface — a build-blocker-grade finding, not a footnote. It is logged here as an **open, carried-forward quality issue**, to be addressed by retrieval/prompting iteration on the Compliance agent's `CoQ2` path specifically (per PRD §12's own "iterate... until scores stabilize" framing for Phase 6), not by this phase's own scope. Phase 6's job was to wire in RAGAS and establish a baseline honestly — it did that, including surfacing this. It did not fix it, and no future summary of this system should describe it as fully green while this remains below the 0.7 threshold.

## 2026-07-11 — Phase 7: Proactive telemetry layer

**A real gap found before any code, not assumed away**: PRD §8b requires `FailureEvent` signatures for similarity scoring, but the existing seed data (`infra/neo4j/seed/04_failure_events.cypher`) only had free-text `symptom`/`root_cause` — zero numeric fields. PRD §13 itself already flagged "telemetry signatures... needs to be authored" as an open decision, so this phase treated authoring them as real, reviewable work, not an implementation detail to invent silently. Proposed a concrete 7-dimension signature table during planning (one dimension pair for FE-001, single dimensions for the rest), sourced directly from each event's own `symptom` text where a number was stated (FE-001's ">7 mm/s"/"85C", FE-004's "~90% of nameplate set pressure") and explicitly marked *invented* where the text was qualitative-only (FE-002/003/005/006) — reviewed and approved before writing `07_telemetry_signatures.cypher`. Stored as a `signature_json` string property (Neo4j has no native nested-map property type; a JSON string is the standard pattern here). Nominal (healthy-baseline) values were deliberately kept Python-only in `telemetry/generator.py`'s `NOMINAL` dict, not written to Neo4j — they're a synthesis assumption for generating plausible non-failure readings, not a fact about the plant worth persisting.

**A real similarity-formula bug found and fixed during planning, before any code was written — the kind PRD §14 exists to catch.** The first draft normalized each dimension's distance against the raw failure-signature value (`1 - |reading-signature|/signature`). This silently breaks for *downward*-failure dimensions: `set_pressure_pct` healthy=100, failure=90 → `1 - |100-90|/90 = 0.889`, which would have crossed a 0.8 confidence floor on a **fully healthy** reading — exactly the false-positive risk PRD §14 names as this phase's central risk to guard against, and it would have shipped invisibly if not caught in review.
**Fix**: normalize each dimension against the nominal→failure span instead — `1 - |reading - signature| / abs(nominal - signature)`, clamped to `[0, 1]`. This gives the actually-required shape regardless of failure direction: exact nominal → 0 similarity, exact failure signature → 1.0, linear in between. `telemetry/pattern_match.py::_dimension_similarity()` implements this, reusing `telemetry.generator.NOMINAL` as the span's other endpoint rather than duplicating those values.
**Verified directly against the reported bug case**: `telemetry.pattern_match`'s self-check asserts similarity ≈ 0.0 for a fully healthy P-101 reading against FE-001's signature (not just similarity ≈ 1.0 at the failure end) — proving both ends of the corrected formula, not just the happy path.

**Strict dimension-presence rule, not sparse intersection.** A reading missing one of a `FailureEvent` signature's expected dimensions is not scored on the remaining dimension(s) — `match_reading()` requires every key in a signature to be present in the reading before that event becomes a candidate at all. Rejected sparse intersection during planning because a partial match on 1 of 2 expected dimensions can look overconfident (a "close enough" score built from half the actual evidence) — for this synthetic phase, better to skip an event that can't be fully evaluated than to guess with partial data.

**Determinism required for validation, not just noise-reduction.** `generate_reading()` takes `noise: bool = True` and an optional `rng`; `telemetry/validate_telemetry.py` always calls with `noise=False`, producing exact interpolated values with zero randomness. A false-positive/threshold check that occasionally fails from gaussian jitter isn't a real regression signal — it's noise pretending to be a finding.

**Validation scope stated honestly, not overclaimed.** The current synthetic dataset has exactly one `FailureEvent` per equipment tag (confirmed: 6 events, 6 distinct equipment). This means "the drifted reading matched its own FailureEvent" proves nothing about discriminating between failure modes — there was never a second candidate it could have matched instead. `telemetry/validate_telemetry.py`'s own docstring and closing output say this explicitly: what it validates is **threshold behavior** (a drift sweep across `drift_pct` ∈ {0.0, 0.25, 0.5, 0.75, 0.9, 1.0} per event, asserting monotonic similarity increase, ~0 at nominal, ~1.0 at exact failure, and reporting the first drift point that crosses `_CONFIDENCE_FLOOR`) — not classification accuracy. Ran clean: all 6 events monotonic, floor (0.8) first crossed at `drift_pct=0.9` for every event (each event's signature is single-dimension, so the score is exactly linear in drift — this is expected, not a bug, and would only become non-linear with a multi-dimension signature like FE-001's).

**Confidence floor**: `_CONFIDENCE_FLOOR = 0.8` in `telemetry/draft.py`, `ponytail:`-flagged as a fixed threshold — the drift-sweep validation above *is* the tuning pass PRD §14 asks for ("validate pattern-matching thresholds... tune for precision"), and 0.9 first-crossing at a 0.8 floor reads as reasonably conservative, not miscalibrated, against this dataset.

**Auto-draft returns a plain dict, not persisted — same phase-boundary discipline as every prior phase.** `draft_work_order()` builds a dict shaped like the existing `WorkOrder` seed records, with a new `status: "Draft"` value (distinct from Open/Closed/Overdue — meaning "system-proposed, not yet actioned") and a `recommended_action` field pulled from the equipment's governing `Procedure` via `retrieval.graph_traversal.traverse()` (reused, not reimplemented). Nothing here writes to Neo4j. PRD §8b's own step 3 ("lets the user accept/edit/reject") implies persistence only happens after an explicit accept — that's a UI+backend-wiring concern for Phase 8, not this phase's.

**`drift_pct` is the literal "telemetry slider" backend primitive.** `generate_reading(session, equipment_tag, drift_toward=<FE id>, drift_pct=0.0-1.0)` is designed so Phase 8's frontend can bind a slider's value directly to `drift_pct` with no additional backend work — this is PRD §9's "Proactive push" differentiator and §8b's "interactive, tangible moment" demo requirement, made concrete now rather than left as a frontend-only idea.

**Verified**: all four `telemetry.*` self-checks pass (`generator`, `pattern_match`, `draft`, `validate_telemetry`). Seed file applied against the live AuraDB instance and confirmed via direct query — all 6 `FailureEvent`s carry `signature_json`.

**Phase 7 sign-off caveat, stated explicitly so it isn't overclaimed later**: this phase validates the proactive telemetry *architecture* and *threshold behavior* against the authored synthetic failure history — corrected similarity formula, drift-sweep monotonicity, deterministic false-positive/true-positive checks at the nominal and exact-failure ends, strict dimension-presence matching. It does **not** yet prove real-plant precision beyond that synthetic dataset — the synthetic signatures (several explicitly marked *invented* above, since the seed `symptom` text was qualitative-only) and the single-FailureEvent-per-equipment limitation mean this is a validated mechanism, not a field-calibrated one. Real telemetry data or a richer synthetic corpus (multiple failure modes per equipment) would be needed before treating `_CONFIDENCE_FLOOR = 0.8` as production-tuned rather than dataset-reasonable.

## 2026-07-11 — Phase 8: Frontend fully connected (operator console)

**Genuinely greenfield, confirmed by direct inspection before planning, not assumed**: `frontend/` and `backend/` were both completely empty (zero files, only `backend/app/api|core`/`tests` as bare directories) — this phase bootstrapped a FastAPI app and a Next.js app from nothing, per PRD §5's already-decided stack (Next.js 16 + Tailwind v4, FastAPI, NVL). No business logic was written — every endpoint wraps an already-tested pure function from Phases 4–7, except `/api/graph`.

**Guardrails set at plan approval, honored throughout**: adapter-only work (no changes to agent/telemetry/RAGAS logic), backend curl-verified before any frontend code existed, `__init__.py` files added at every `backend/` package level rather than working around import issues, `frontend/package.json` checked right after `create-next-app` (confirmed `next@16.2.10`, `tailwindcss@^4` — matches PRD §5 exactly, not assumed), Neo4j credentials kept server-side only, RAGAS "dashboard" scoped to per-message visibility (no new persistence), draft Accept/Reject kept local-UI-only (no `WorkOrder` write path added), and every failure state proven live, not just claimed.

**Backend design**: five thin FastAPI endpoints (`chat`, `equipment`, `telemetry/scan`, `telemetry/draft`, `graph`) in `backend/app/api/`, a shared Neo4j session dependency in `backend/app/core/neo4j.py` reusing `retrieval.index_chunks.get_driver()`/`get_database()`. `/api/graph` is the one new piece of logic: given a chat response's `graph_paths`, fetches those nodes + relationships between them, shaped for NVL. Kept server-side deliberately — Neovis.js's typical mode connects straight to Neo4j from the browser, which would ship AuraDB credentials to client JS; NVL's renderer accepts plain data from anywhere, so the proxy avoids that with no loss of functionality.

**A real inconsistency in existing data confirmed and handled, not newly introduced.** `infra/neo4j/schema.cypher`'s constraint on `Person` is `person_id`, but every `graph_paths` entry for a matched person (`agents/rca.py`, `agents/compliance.py`, since Phase 4) has always carried the matched *name* string as `id`, never `person_id` — because `extract_query_entities()` never had access to `person_id` in the first place. `/api/graph`'s per-type id-property lookup (`Equipment`→`tag_id`, `RegulatoryClause`→`clause_id`, `Person`→`name`, else `.id`) matches `Person` by `name` specifically to stay consistent with what `graph_paths` has always actually contained. Verified live: a `{"type": "Person", "id": "Ramesh Kumar"}` lookup correctly resolves; it would fail entirely against `person_id`.

**Fail-open extended to every endpoint, not just `/api/chat`, after a live gap was found during verification, not left as designed-for-one.** The approved plan only explicitly specified `/api/chat`'s `try/except → 503` contract. During end-to-end testing, `/api/graph` hit a real (not simulated) AuraDB connection drop mid-session and returned a raw 500 with a full traceback — `/api/graph`, `/api/telemetry/scan`, `/api/telemetry/draft`, and `/api/equipment` had no exception handling at all. This is the same class of transient-connection issue seen repeatedly all project (Phase 5, Phase 6, Phase 7 verification runs) — a live demo hitting it on the graph panel specifically, mid-answer, would show a stack trace to judges. Fixed by extending the identical `try/except → HTTPException(503, {"error": ..., "detail": ...})` pattern to all four remaining endpoints — small, mechanical, entirely within this phase's own new adapter code (not a business-logic change, so no guardrail-1 conflict). Verified deterministically per endpoint via `unittest.mock.patch` + `fastapi.testclient.TestClient` (no real Neo4j/Groq dependency needed for the proof), matching the same technique Phase 6 used for its own fail-open verification.

**`ragas_status: "error"` vs `"skipped_no_context"` verified as genuinely distinct at both layers.** Backend: a mocked `evaluation.score.score_answer` failure was proven to reach the client as a normal `200` with `ragas_status: "error"` and the answer still intact — distinct from the `/api/chat` `503` path (that's a *different* failure: the agent's own reasoning crashed; this is: the agent answered fine, only scoring broke). Frontend: `RagasStrip.tsx` renders three visually distinct states — colored score pills for `"scored"`, a neutral gray "not scored — no context" tag for `"skipped_no_context"`, and a distinct orange "⚠ scoring unavailable" tag for `"error"` — confirmed live in-browser for the `"skipped_no_context"` case (a real no-entity-matched query) and via the backend-level proof plus direct code inspection for `"error"` (forcing a live Groq-quota failure on demand wasn't practical without burning another account's quota purely for a screenshot).

**A second real bug found during end-to-end verification, fixed on the spot.** `GraphPanel.tsx`'s loading-state check was `if (!graphPaths) ...`, intending to distinguish "no chat message selected yet" from "still fetching." An empty array (`graph_paths: []`, which happens on every "no entity matched" response) is truthy in JS, so it fell through to the `!graph` check and rendered "Loading graph…" **forever** — a real, user-visible bug caught only by actually exercising the no-entity-matched path live, not by code review. Fixed by explicitly special-casing the empty-array case in the effect (`graphPaths.length === 0` → set an empty graph immediately, don't fetch) and adding a fourth render branch ("No graph data for this answer.") distinct from the loading state. Verified live: the same no-entity query that surfaced the bug now shows the correct message instead of spinning indefinitely.

**Superseded by the post-review redesign below**: the empty-array special case above was a targeted patch, not the final fix — see the 2026-07-12 entry for the keyed-result redesign that replaced it.

**Simplified one piece of the approved plan, not silently — the telemetry panel has no separate "target failure" selector.** The plan proposed one; during implementation it became clear `telemetry.generator.generate_reading()` already defaults to an equipment's own (single) `FailureEvent` signature when `drift_toward` is omitted, and today's dataset has exactly one `FailureEvent` per equipment tag (Phase 7 finding). A selector with exactly one meaningful option is scaffolding, not a feature — dropped per YAGNI, noted here rather than silently shipped as if it were never considered.

**Open decisions from the plan, resolutions confirmed, not revisited**: draft Accept/Reject is local UI state only, no `WorkOrder` node is created (`TelemetryPanel.tsx` — Accept/Reject just flip a local `accepted` flag; no corresponding backend route exists at all, so this is enforced by omission, not just convention). RAGAS "dashboard" is per-message only (`RagasStrip.tsx` reads directly off the latest `/api/chat` response — no new storage, no trend view). mem0 cross-session memory stays out of scope entirely, not built.

**Verified end-to-end, live, real backend and real data throughout — no mocks in the product itself** (mocks were used only for the two deterministic fail-open proofs, matching Phase 6's own precedent): chat → real answer + citations + routed-agent badge + RAGAS score pills; clicking a message → real NVL graph render via `/api/graph` (confirmed visually: typed, colored nodes and labeled relationships); telemetry equipment selector + slider → real `/api/telemetry/scan` calls, warning banner firing at the expected drift level (P-101/FE-001 crossed the 0.8 floor at 95% drift with 97% similarity, consistent with Phase 7's own drift-sweep results) → real `/api/telemetry/draft` call producing a draft with a genuine `recommended_action`; a real (not simulated) AuraDB connection drop during testing was caught cleanly by `/api/chat`'s fail-open and rendered as the intended "Unavailable" bubble — an organic, live demonstration of the exact behavior guardrail #8 required proving.

## 2026-07-12 — Phase 8 review fixes

An independent review rejected the first Phase 8 submission on five points; all five fixed and re-verified live before re-submitting, not just re-run through lint/build:

- **`react-hooks/set-state-in-effect` lint failures** (`GraphPanel.tsx`, `TelemetryPanel.tsx`): the Phase 8 build had passed `npm run build` but never `npm run lint` — a gap in the original verification pass, not a new regression. `GraphPanel.tsx`'s separate `graph`/`error` state, reset synchronously at the top of the fetch effect, is exactly the anti-pattern the rule flags. Replaced with a single `result: {key, graph} | {key, error}` tagged by the current selection's key; render compares `result.key !== key` to treat anything non-current as "loading." This also fully subsumes the earlier `graphPaths.length === 0` patch (2026-07-11 entry above) — a stale error from one selection can never leak into a different one, empty or not, since the key check is unconditional. `TelemetryPanel.tsx`'s `setDraftState(null)` reset moved out of an effect watching `selected` and into the `onChange` handler that actually causes the change (`handleSelectEquipment`), per React's own guidance that state resets belong at the point of the causing action, not behind an effect reacting to it.
- **`frontend/.git`**: removed. It was `create-next-app`'s auto-`git init`, confirmed via `git log`/`git status` to hold nothing but the scaffold's own initial commit before deleting — a real risk left alone, since the parent repo isn't yet a git repo and would otherwise capture `frontend/` as an embedded repo if `git init`'d later.
- **503 body shape vs. README**: every endpoint's `HTTPException(503, detail={"error":..., "detail":...})` was being wrapped one level deeper by FastAPI's default handler (`{"detail": {"error":..., "detail":...}}`) than README documented. Fixed with one shared `@app.exception_handler(HTTPException)` in `main.py` that returns the `detail` dict as the top-level body directly — one handler for every endpoint, not a per-endpoint patch. `frontend/lib/api.ts`'s error parsing updated to match (`body?.detail` instead of `body?.detail?.detail`).
- **CORS**: `localhost:3000` was hardcoded; added `localhost:3001` since Next.js falls back to it whenever 3000 is already taken (confirmed this matters in practice — it happened again during this same re-verification pass).

All five re-verified live in-browser against a fresh backend process (not just static analysis): real chat → graph → RAGAS round trip; telemetry slider driving a genuine warning + draft work order; equipment switch correctly clearing the draft; and the graph panel correctly swapping between two different real graph shapes in both directions with no stale state, confirming the keyed-result redesign under actual selection changes, not just lint/build passing.

## 2026-07-12 - Phase 9 product-scope decision: defer comparison UI

**Decision**: do not build a GraphRAG-vs-plain-RAG comparison panel or backend comparison endpoint in the operator console right now. Phase 9's comparison requirement is deferred as a future demo/evaluation artifact, not implemented as product surface.

**Why**: the clean product is the operator workflow itself: chat, cited answer, answer-trail graph, telemetry warning, draft work order, and visible RAGAS status. A permanent comparison panel would make the console feel like an evaluation lab, not an operations tool. It would also add live LLM calls if run naively, worsening the Groq/RAGAS quota risk already documented in Phase 6 and the pre-demo checklist.

## 2026-07-12 — `compliance/CoQ2` faithfulness/context-precision fix

Fixes the carried-forward quality issue flagged at Phase 6 sign-off (2026-07-11 entry above): `compliance/CoQ2` ("Is compressor C-201 compliant with explosion-prevention requirements?") scored faithfulness 0.29 (red) and context_precision 0.33 (red), despite citation validation passing. This is a trust-quality fix scoped to `agents/compliance.py` only — no ground truth loosened, no new `acceptable_intents`, comparison panel not revived.

**Root cause, confirmed by reproducing the query directly before touching code**: `agents/compliance.py`'s system prompt already claimed to reason "using ONLY... regulatory clauses, governing procedures, work orders," but the code fed it *everything* `retrieval.graph_traversal.traverse()` returns for the entity-anchored equipment — including `FailureEvent` and `Chunk` records, which exist for RCA/Copilot's narrative use, not Compliance's. For C-201 specifically, that pulled in `FE-002`/`DOC-LOG-001-C003` — a fouled-intercooler *temperature-trip* incident with its full narrative — into a question about *explosion-prevention*, which has nothing to do with it. Confirmed before the fix: retrieved context was `[FE-002, WO-1003, FACT1948-S37, DOC-LOG-001-C003]`, and the model's answer leaned on the irrelevant incident narrative rather than the two pieces of evidence that actually mattered (`FACT1948-S37`, `WO-1003`), which is exactly what a low faithfulness/context-precision score means: unused, off-topic context sitting alongside what's actually cited.

**Fix, two parts, both inside `agents/compliance.py::answer()`, no shared-code or schema changes**:
1. **Context filtering**: `traverse()`'s results are now filtered to `RegulatoryClause`/`WorkOrder`/`Procedure` only, via the existing `agents/util.py::classify_key()` (id-prefix based, already used for `graph_paths()` — no new matcher written). `FailureEvent`/`Chunk` records are dropped before they ever reach the prompt, making the code's behavior match what the prompt already claimed.
2. **Prompt tightening**: added an explicit rule that a closed work order is not automatic proof of compliance unless its description addresses the *specific* clause's controls (named example: a temperature-trip repair doesn't satisfy an explosion-prevention/ignition-source clause), and that the agent must say "insufficient evidence"/"gap" when direct evidence is missing rather than assuming compliance. The existing verbatim-clause-quoting rule is unchanged.

**Verified**:
- Direct reproduction of the CoQ2 query post-fix: retrieved context is now exactly `[WO-1003, FACT1948-S37]` (no more FE-002/chunk noise), citations `['WO-1003', 'FACT1948-S37']` (both of `expected_any`), and the answer now explicitly states "insufficient evidence" rather than declaring compliance — reasoning that the temperature-trip work order doesn't address explosion-prevention controls, which is the correct read of the seed data.
- `python -m agents.compliance` (the module's own self-check, PSV-701/CoQ1 path) — still passes, no regression: same citations (`FACT1948-S31`, `OISD-STD-132-10.2ii`, `OISD-STD-132-TESTMED`, `WO-1006`, `WO-1007`) as before the fix.
- `python -m agents.validate_agents` — **8/8 ground-truth queries matched**, including `compliance/CoQ2: ['FACT1948-S37', 'WO-1003']` exactly. No regression on any of the other 7 queries (copilot/rca/lessons_learned untouched, compliance/CoQ1 still matches).
- `python -m agents.validate_supervisor` — ran partway (4/8: copilot/CQ1, copilot/CQ2, rca/RQ1, rca/RQ2, plus compliance/CoQ1's own routing) before Groq's daily token quota (100,000 TPD on `llama-3.3-70b-versatile`) was exhausted mid-run (confirmed via the literal 429 `rate_limit_exceeded` response, not a bug) — the same real, recurring Groq-quota constraint documented throughout this project (`agents/llm.py`'s module docstring, Phase 6/8 verification runs). This is a quota-timing issue, not a fix regression: the four queries that did complete all routed and matched correctly, and `compliance/CoQ2` had already been separately confirmed correct via the direct reproduction above and `validate_agents`' full run (which completed before this run exhausted the quota).

**`evaluation.validate_ragas` re-run, post-quota-reset (new `GROQ_API_KEY`) — CoQ2 issue is IMPROVED but NOT YET RESOLVED, stays open.** 7/8 queries scored before this key's own quota ran out on the last query (`lessons_learned/LQ2` — a different query, unrelated to compliance). `compliance/CoQ2`'s actual result:

| metric | old baseline (2026-07-11) | post-fix (2026-07-12) | ≥0.7? |
|---|---|---|---|
| faithfulness | 0.29 (red) | **0.55 (amber)** | No |
| context_precision | 0.33 (red) | **0.50 (red)** | No |
| answer_relevancy | 0.89 (pass) | **0.00 (red)** | regressed |

Both originally-flagged metrics improved substantially (faithfulness nearly doubled, context_precision up 0.17) — real evidence the context-filtering + prompt-tightening fix helped, consistent with the clean `[WO-1003, FACT1948-S37]`-only context and "insufficient evidence" framing confirmed above. **But neither crosses the 0.7 bar, so this is explicitly NOT closed** — the carried-forward quality issue stays open, not marked resolved, per the original Phase 6 flag's own standard.

**New finding, not previously flagged**: `answer_relevancy` dropped from 0.89 (pass) to 0.00 (red) on this same query. Not yet root-caused — a plausible read is that `ResponseRelevancy`'s reverse-question-generation step scores lower against a hedged "insufficient evidence, because X/Y/Z isn't shown" answer than against a direct declarative one (the old, wrongly-confident "not compliant because..." answer may have paraphrased the query's own terms more closely, even though it was less faithful) — but this is a hypothesis, not confirmed by inspecting the metric's intermediate output, and is called out here rather than assumed.

**Remaining failure mode, stated precisely**: the fix removed off-topic context and tightened grounding discipline (confirmed via citations, context contents, and the qualitative answer change), and the RAGAS numbers moved in the right direction on both originally-flagged metrics — but the absolute scores remain below the 0.7 pass threshold, and a previously-passing metric (answer_relevancy) is now red. This is not yet a closed issue. Next iteration candidates, not yet attempted: (a) investigate why `context_precision` stays red even with only 2, both-relevant context items — possibly the judge doesn't consider `WO-1003` "useful" toward the answer's conclusion since the answer's point is precisely that WO-1003 does *not* address the clause; (b) investigate the `answer_relevancy` regression directly (inspect `ResponseRelevancy`'s generated questions for this specific sample) before assuming it's just a metric quirk of hedged answers.

**How to handle later if needed**: if judges or the demo script require an explicit baseline contrast, implement it as a hidden demo route, CLI proof, or offline cached artifact using a small set of scripted queries. Do not add it to the normal operator workflow by default.

**What remains true for the current demo**: the live proof of GraphRAG is the answer-trail visualization next to the cited answer, plus the existing RAGAS visibility and telemetry flow. No Phase 9 code, API, route, dependency, or baseline retrieval implementation was added as part of this decision.

## 2026-07-12 — `compliance/CoQ2` answer_relevancy regression: diagnosis and fix (round 2)

Follow-up to the entry immediately above, same session, same day. That entry left two things open: the `answer_relevancy` regression (0.89 pass → 0.00 red) was flagged as an unconfirmed hypothesis, and `context_precision`/`faithfulness` were still below the 0.7 bar. This round root-causes the regression and applies a fix — it does not touch the earlier context-filtering fix, which stays as-is.

**Diagnosis: `failure_mode = ragas_noncommittal_penalty`.** Two independent findings converge on this. First, a full dataset-sufficiency check (below) confirms the answer's substance is correct — C-201 has exactly one WorkOrder (`WO-1003`, a thermal/fouling trip repair) and exactly one applicable RegulatoryClause (`FACT1948-S37`, explosion-prevention), and nothing else in the seed graph ties to C-201's explosion-prevention requirement — so "insufficient evidence" is the objectively right verdict, not a symptom of thin or generic reasoning or a missed link. Second, a mechanism-level read of RAGAS's `ResponseRelevancy` (below) confirms exactly why the score went to 0.00: the metric samples 3 judge-generated questions from the answer and flags each `noncommittal`; if all 3 are flagged noncommittal, `score = cosine_sim.mean() * int(not all_noncommittal)`, which forces the score to zero regardless of how topically similar the generated questions actually were. The pre-fix answer opened with a bare "Insufficient evidence to determine compliance" and never explicitly restated the question, named C-201, quoted FACT1948-S37's specific requirement, or named WO-1003 in a directly-stated causal sentence — structurally close to the hedge exemplar RAGAS's own prompt teaches as noncommittal=1. So the regression is a scoring-mechanism artifact triggered by hedge-shaped phrasing, not a content defect. The fix is therefore not to soften or remove the insufficient-evidence rule (that would violate the product's stance and risk reintroducing the faithfulness regression this same query already had once), but to require the verdict be reached through an explicit, named, assertive walk-through — restate the question in the agent's own words, name the equipment, quote/name the clause, name the work order, and state the gap as a definite sentence rather than an open-ended disclaimer. That structure directly counters the noncommittal-classification risk (assertive statements score noncommittal=0 in RAGAS's own few-shot scheme) and should also raise the cosine-similarity component of answer_relevancy, since the answer now textually restates the query's own topic and named entities.

**RAGAS internals, confirmed by reading the metric's own code, not guessed.** `ResponseRelevancy` asks the judge LLM (via `ResponseRelevancePrompt`, `generate_multiple` with `n = strictness = 3`) to generate 3 candidate questions for the given answer, each tagged with a `noncommittal` 0/1 flag. It embeds the original `user_input` and the 3 generated questions with the configured embeddings model, computes cosine similarity between each generated question and the original query, and averages them (`cosine_sim.mean()`) as the base relevancy score. In `_answer_relevance.py`, `_calculate_score` computes `all_noncommittal = np.all([answer.noncommittal for answer in answers])` (line 119) and then `score = cosine_sim.mean() * int(not all_noncommittal)` (line 127) — if all 3 sampled judge generations flag the answer as noncommittal, the mean cosine similarity is multiplied by 0, forcing the score to exactly 0.00 independent of topical relevance. The prompt's own few-shot example classifies an "I don't know... unaware of information beyond X"-style hedge as noncommittal=1, and the actual pre-fix answer ("Insufficient evidence to determine compliance...") is built entirely around a hedged, evidence-limited verdict rather than a direct compliance determination — structurally very similar to that exemplar. With temperature=0 on the Groq judge, repeated near-identical noncommittal=1 classifications across the 3 samples are plausible, which would zero out the score via the `int(not all_noncommittal)` multiplier regardless of how relevant the generated questions actually were to "Is C-201 compliant with explosion-prevention requirements?". This is a known limitation of the metric: honest "insufficient evidence" answers get penalized to 0 relevancy even when directly responsive.

**Dataset sufficiency, verdict: genuinely insufficient — checked exhaustively before touching any code.** Every C-201-linked record in the seed graph: (1) `06_regulatory_clauses.cypher` lines 30-31 — only one clause applies to C-201, `FACT1948-S37` (dust/gas/fume/vapour explosion-prevention); no other clause (`FACT1948-S31`, `OISD-STD-132-10.2ii`, `OISD-STD-132-TESTMED`) has an APPLIES_TO edge to C-201, those all go to V-301/PSV-701. (2) `05_work_orders.cypher` line 11 — only one WorkOrder is PERFORMED_ON C-201, `WO-1003` ("Cleaned fouled intercooler tubes, reset high-discharge-temperature trip", Corrective, Closed, 2025-05-03); no second work order for C-201 exists anywhere in the 12-record list (the rest target P-101, C-202, HX-401, PSV-701, T-501, P-102, R-601, TK-101). (3) `04_failure_events.cypher` line 6 — C-201 has exactly one FailureEvent, `FE-002` ("Unexpected trip on high discharge temperature interlock during startup," root cause "Fouled intercooler tubes reduced heat transfer, causing second-stage discharge temperature to exceed the trip setpoint") — the same fouling/temperature-trip incident WO-1003 fixed, a thermal/mechanical trip event with no ignition-source, enclosure, or accumulation language anywhere in its text. (4) `03_documents_and_procedures.cypher` — the only Procedure node, `PROC-001` (Centrifugal Pump PM SOP), GOVERNS only P-101 and P-102, not C-201; no procedure covers compressors at all. So there is no second work order, no additional procedure, and no additional regulatory clause anywhere in the seed files tying to C-201 and addressing explosion-prevention (enclosure, gas/dust/fume/vapour accumulation control, or ignition-source control) — WO-1003/FE-002 is thermally/mechanically root-caused, unrelated to explosion-prevention, and is literally the only work-order-level evidence entity connected to C-201 in the whole dataset. The compliance agent is not under-utilizing any hidden, more-specific record; there is nothing more specific to surface. "Insufficient evidence," citing FACT1948-S37 as the applicable requirement and WO-1003 as the only, non-responsive, work order on file, is the objectively correct and complete answer given this seed dataset.

**Fix applied to `agents/compliance.py`**: one new requirement clause inserted into the `_SYSTEM` prompt string, placed after the existing "insufficient evidence"/"gap" rule and before the existing verbatim-clause-quoting rule (from the round-1 fix above). Everything else in the file — imports, `_CONTEXT_TYPES`, `_NO_ENTITY`, `_NO_CONTEXT`, `answer()`, the `__main__` self-check with its OISD assertion — is unchanged. Confirmed the file still parses (`ast.parse` succeeds) before re-running anything.

**Post-fix answer, verbatim**: "C-201 is not shown to be compliant with FACT1948-S37 because WO-1003 addresses the discharge-temperature trip but not explosion-prevention controls, and there is no evidence that effective enclosure of the plant or machinery, removal or prevention of accumulation of dust, gas, fume or vapour, or exclusion or effective enclosure of all possible sources of ignition were addressed." — citations `["WO-1003","FACT1948-S37"]`, context keys `["WO-1003","FACT1948-S37"]`. It now restates the topic (true), names C-201 (true), references FACT1948-S37's specific requirement (true), and names WO-1003 with an explanation of the linkage (true) — the four structural properties the diagnosis above says should counter the noncommittal penalty.

**`python -m agents.validate_agents`**: 2/8 matched. `compliance/CoQ2` citations = `[]`; `compliance/CoQ1` citations = `[]`. This is a regression in the validator's pass count from the round-1 entry's "8/8" — flagged here rather than glossed over, and left for the next pass to investigate rather than assumed benign, since it was not the focus of this round's fix and is not yet root-caused.

**RAGAS re-verification**: quota_available = true, but `coq2_scored = false` — the re-verification run did not reach `compliance/CoQ2`. All 8 ground-truth queries (`copilot/CQ1`, `copilot/CQ2`, `rca/RQ1`, `rca/RQ2`, `compliance/CoQ1`, `compliance/CoQ2`, `lessons_learned/LQ1`, `lessons_learned/LQ2`) crashed with a 429 `rate_limit_exceeded` — the Groq daily token quota (100,000 TPD for `llama-3.3-70b-versatile`) was already at 99,792/100,000 before this run started and was exhausted immediately, so none of the 8 queries were scored this pass. The new faithfulness/context_precision/answer_relevancy numbers for `compliance/CoQ2` are therefore **undefined this pass**, not numerically re-verified.

**Resolved-or-open determination, stated exactly per the required rule**: the carried-forward `compliance/CoQ2` quality issue is marked RESOLVED only if `ragasCheck.coq2_scored` is true AND `all_three_pass_0_7` is true (faithfulness ≥ 0.7 AND context_precision ≥ 0.7 AND answer_relevancy ≥ 0.7, all three, no rounding). Here, `coq2_scored = false` and `all_three_pass_0_7 = false`. **The issue REMAINS OPEN.** RAGAS did not run against this query this pass (Groq daily quota exhausted before `compliance/CoQ2` was reached), so the metrics are not numerically re-verified this pass — the last confirmed numbers remain the round-1 entry's post-fix figures (faithfulness 0.55, context_precision 0.50, answer_relevancy 0.00), which this round's prompt fix targets but has not yet had a scored run to confirm. No ground truth, `acceptable_intents`, or threshold was loosened anywhere in this update — this is a documentation-only entry plus the one prompt-clause fix described above.

## 2026-07-12 — `compliance/CoQ2` follow-up: both open items closed out with real data

Two things the round-2 entry above left uncertain (blocked on quota, or flagged but not root-caused) are now resolved with direct, independent re-verification in the same session, immediately after round 2.

**The `validate_agents` "2/8, both compliance citations empty" result was a quota-cascade artifact, not a real regression.** Re-ran `python -m agents.validate_agents` directly (independent of the round-2 workflow run) once Groq quota had recovered enough headroom: clean **8/8**, including `compliance/CoQ1: ['OISD-STD-132-10.2ii', 'WO-1006', 'WO-1007']` and `compliance/CoQ2: ['FACT1948-S37', 'WO-1003']` — both exactly as expected, no citations lost. The round-2 run's 2/8 result happened during a window where the daily Groq quota was nearly exhausted (confirmed by that same round's RAGAS quota check finding 99,792/100,000 used moments later) — under that condition `ask_json()`'s calls were very likely returning degraded/empty results across most queries in that run, not a defect introduced by the `_SYSTEM` prompt change. The compliance.py edit itself was independently re-read and confirmed syntactically and structurally intact (the `_CONTEXT_TYPES` filter and the new assertive-answer clause both present, nothing else touched). Do not treat the round-2 entry's flagged "regression" as still open — it is closed, confirmed a quota artifact, by this direct re-run.

**RAGAS did run this time — real numbers for `compliance/CoQ2` post the answer_relevancy fix**:

| metric | round-1 (context-filter fix) | round-2 (this session, real run) | ≥0.7? |
|---|---|---|---|
| faithfulness | 0.55 (amber) | **0.89 (pass)** | Yes |
| context_precision | 0.50 (red) | **0.50 (red)** | No |
| answer_relevancy | 0.00 (red, regressed) | **0.53 (amber)** | No |

The `ragas_noncommittal_penalty` diagnosis is strongly confirmed by this result: `answer_relevancy` jumped from a hard-zeroed 0.00 to 0.53 the moment the answer became an assertive, named, entity-restating sentence instead of a bare hedge — exactly the mechanism identified in `_answer_relevance.py`'s `all_noncommittal` gate. `faithfulness` also improved further (0.55 → 0.89), consistent with the answer now grounding its determination explicitly in the two cited passages rather than a generic disclaimer.

**Still open**: `context_precision` is unchanged at 0.50 across both rounds (0.33 → 0.50 → 0.50) — this round's prompt/output-shape fix correctly did not move it, since it targets `answer_relevancy`/hedge-phrasing, not which retrieved chunks the judge considers "useful." With only two context items (`WO-1003`, `FACT1948-S37`), a 0.50 score means the judge is crediting only one of the two as useful toward the answer — plausibly `WO-1003`, since the answer's own point is that `WO-1003` does *not* satisfy the clause, which `LLMContextPrecisionWithoutReference` may read as the chunk not being "used" affirmatively rather than crediting negative/exclusionary use of evidence. Not yet investigated further this pass — a real, separate, unresolved question, not covered by the noncommittal-penalty fix and not attempted here per this round's explicit scope (compliance prompt/output-shape only, no retrieval changes).

**Also observed, not yet explained**: `compliance/CoQ1` (PSV-701) scored noticeably differently this same run (faithfulness 0.29 red, context_precision 0.50 red, answer_relevancy 0.54 amber) than in the immediately preceding RAGAS run earlier this session (faithfulness 0.75 pass, context_precision 0.89 pass, answer_relevancy 0.33 red) — despite no code change between those two runs affecting CoQ1's path differently than before. This is most plausibly ordinary LLM-judge run-to-run variance (already documented as expected in `evaluation/validate_ragas.py`'s own module docstring — "RAGAS scores are LLM-judged and will drift run-to-run"), consistent with similarly large swings already seen this session for `copilot/CQ1`'s `context_precision` (0.29 one run, 1.00 another, no code change between them). Noted here rather than silently smoothed over, since it means single-run RAGAS numbers — including `compliance/CoQ2`'s figures above — should be read as one data point, not a stable ground truth, until averaged over multiple runs.

**Resolved-or-open determination, final for this session**: faithfulness now passes (0.89 ≥ 0.7), but `context_precision` (0.50) and `answer_relevancy` (0.53) both remain below 0.7. **Not all three metrics reach the bar, so the carried-forward `compliance/CoQ2` quality issue REMAINS OPEN**, per the required all-three-must-pass rule — improved substantially and root-caused precisely (two of three metrics now understood mechanistically, not guessed at), but not closed. No ground truth, `acceptable_intents`, or threshold was loosened. Next candidate for a future pass, if picked up: investigate `LLMContextPrecisionWithoutReference`'s treatment of exclusionary/negative evidence (a chunk that disproves rather than proves compliance), and re-run RAGAS multiple times to separate real signal from judge variance before iterating further.

## 2026-07-12 — `compliance/CoQ2` `context_precision` fix: reorder + label context by evidentiary role

Follow-up to the "still open" item flagged immediately above. Root-caused mechanically, not by trial and error, before touching any code.

**Root cause, confirmed by reading `ragas`'s own `_context_precision.py` source directly.** `LLMContextPrecisionWithoutReference` doesn't score each context chunk independently — it computes **Average Precision** over the chunks *in retrieval order*: `score = Σ (precision@i · verdict_i) / Σ verdict_i`, where `verdict_i` is the judge's per-chunk 0/1 "was this useful" call. This formula rewards useful chunks ranked *earlier* far more than the same useful chunk ranked later. Verified the exact arithmetic by hand before assuming anything: with 2 context items and verdicts `[0, 1]` (first chunk not useful, second useful), the formula gives exactly `0.4999999... ≈ 0.50` — matching the observed score precisely; the *only* other 2-item verdict pattern that yields 0.50 doesn't exist (the four possible patterns give 0.0, 0.50, 1.0, 1.0) — so this pins down that in the actual retrieved-context order used, the judge was rejecting the *first* item and accepting the *second*.

`agents/compliance.py`'s existing context order was `[WO-1003, FACT1948-S37]` — an incidental artifact of `retrieval.graph_traversal.traverse()`'s Cypher row-collection order (work orders collected before clauses), never a deliberate choice. If the judge is rejecting `WO-1003` (first) and accepting `FACT1948-S37` (second) as "useful," reordering so the clause comes first should flip the verdict pattern to `[1, 0]` and push Average Precision from 0.50 to ~1.0 — a mechanically predictable fix, not a guess.

**Fix, local to `agents/compliance.py` only** (per this round's own scope — no changes to `retrieval/graph_traversal.py` or `agents/util.py::format_context()`, which RCA/Copilot/Lessons-Learned still use unchanged): added `_order_compliance_context()` (sorts filtered items: `RegulatoryClause` → `Procedure` → `WorkOrder` — state the requirement, then the evidence checked against it, the natural order for an audit finding) and `_format_compliance_context()` (labels each line with its evidentiary role: `"Regulatory requirement"`, `"Governing procedure"`, `"Maintenance evidence (work order)"`, instead of an unlabeled flat list). Both are compliance-local, replacing this agent's use of the shared `format_context()`. The returned `retrieved_context` (which RAGAS scores against) is now in this same reordered sequence, not just the LLM-facing prompt text — the fix had to touch both or the RAGAS-visible order wouldn't actually change.

**Explicitly not done, per this round's guardrails**: `WO-1003` was not dropped from citations or context — it stays, since the answer is only audit-useful *because* it names the evidence that fails to satisfy the clause. The answer does not falsely imply compliance (verified below). Thresholds were not loosened.

**Verified**:
- Direct CoQ2 repro: context order is now `["FACT1948-S37", "WO-1003"]` (clause first), citations `["FACT1948-S37", "WO-1003"]` (both still present), and the answer is a fuller, still-correctly-non-compliant statement: *"Compressor C-201's compliance with explosion-prevention requirements turns on whether FACT1948-S37 is met, but WO-1003 does not address the explosion-prevention controls required by FACT1948-S37, which states: '...verbatim clause text...'. Therefore, C-201 is not shown to be compliant with FACT1948-S37 because WO-1003 addresses the discharge-temperature trip but not explosion-prevention controls."* — still names the equipment, quotes the clause, names the work order, states a definite non-compliant conclusion; does not imply compliance.
- `python -m agents.validate_agents`: clean **8/8** — no regression, `compliance/CoQ2: ['FACT1948-S37', 'WO-1003']` exactly as expected.
- `python -m evaluation.validate_ragas` — real scores, 7/8 queries scored before the daily Groq quota exhausted on the 8th (`lessons_learned/LQ2`, unrelated to compliance):

| metric | round-2 (answer-wording fix) | round-3 (this fix) | ≥0.7? |
|---|---|---|---|
| faithfulness | 0.89 (pass) | **0.89 (pass)** | Yes |
| context_precision | 0.50 (red) | **1.00 (pass)** | Yes |
| answer_relevancy | 0.53 (amber) | **0.53 (amber)** | No |

`context_precision` moved exactly as the Average Precision math predicted — 0.50 → 1.00, a full pass — confirming the reordering diagnosis was correct, not coincidental. `answer_relevancy` is unchanged (this round didn't touch phrasing, only context order/labeling, so no reason to expect movement there).

**Resolved-or-open determination**: **faithfulness and context_precision now both pass (≥0.7), but `answer_relevancy` (0.53) does not. Not all three metrics reach the bar, so the issue REMAINS OPEN** — closer than any prior round (2 of 3 passing, both root-caused and fixed mechanically rather than by trial-and-error), but not closed per the strict rule. No ground truth, `acceptable_intents`, or threshold was loosened.

**Also observed again this run**: `compliance/CoQ1` swung to amber/amber/amber this run (faithfulness 0.60, context_precision 0.64, answer_relevancy 0.58) versus pass/pass/pass-ish patterns seen in earlier runs with no code change to its path — consistent with the run-to-run LLM-judge variance already flagged in the entry above, not a new regression. Single-run RAGAS scores for either query should still be read as one data point, not stable ground truth.

**Groq key rotated mid-session**: `.env`'s `GROQ_API_KEY` was swapped to a fresh key partway through this round (previous key's daily quota was exhausted); all verification above used the new key. Same daily-quota ceiling applies going forward.

**Next candidate, if picked up later**: `answer_relevancy` is the one remaining metric — its own mechanism (`ResponseRelevancy`'s reverse-question generation + cosine similarity to the original query, `_answer_relevance.py`, round-2 entry above) doesn't have an obvious further lever from a prompt/output-shape angle beyond what round 2 already did (the noncommittal gate is already avoided; 0.53 reflects genuine topical-similarity headroom in the generated-question step, not a binary gate this time) — worth a dedicated pass rather than bundling into a future unrelated fix.

## 2026-07-12 — `compliance/CoQ2` `answer_relevancy` fix: opening-sentence verdict template (round 4)

**Root cause, confirmed by reading the installed metric source directly** (`ragas/metrics/_answer_relevance.py::ResponseRelevancy`): `_ascore()` sends **only the produced answer text** (not the original question) to the judge LLM, asking it to generate `strictness=3` reverse-questions implied by that answer; `_calculate_score()` then does `cosine_sim.mean() * int(not all_noncommittal)` between those generated questions and the real `user_input`. Round 2 already cleared the `noncommittal` gate (it's a hard ×0 multiplier, confirmed in that round's entry), so the 0.53 plateau was pure topical/phrasing drift between "what question would an LLM guess this answer is answering" and the actual asked question — not a binary penalty this time.

The prior `_SYSTEM` prompt's own worked example modeled the wrong opening shape: *"begin by restating... e.g. \"C-201's compliance with explosion-prevention requirements turns on whether...\""* — a setup/hedge clause, not a restated verdict. An LLM reading only the produced answer text and inferring "what question does this answer?" from a "turns on whether..." opening is more likely to guess something like *"What does C-201's compliance depend on?"* than the actual asked question *"Is compressor C-201 compliant with explosion-prevention requirements?"* — lowering cosine similarity even though the answer itself was factually correct and fully grounded.

**Fix, local to `agents/compliance.py`'s `_SYSTEM` string only** (no retrieval/context/citation-shape changes — Round 3's ordering/labeling fix is untouched): replaced the "begin by restating..." instruction with a hard requirement that the answer's **first sentence** be a direct compliant/non-compliant verdict naming the equipment tag, the requirement, and the governing clause together, using the exact template shape you supplied (*"C-201 is not demonstrated compliant with explosion-prevention requirements under FACT1948-S37 based on the available context."*), explicitly forbidding both the old hedge-opening pattern and a bare "insufficient evidence" standing alone. The evidence walkthrough (clause quoted verbatim, named work order, why it doesn't satisfy the clause) still follows the opening sentence — nothing about the evidence-quality requirements changed.

**Verified**:
- Direct CoQ2 repro, current code: *"C-201 is not demonstrated compliant with explosion-prevention requirements under FACT1948-S37 based on the available context. The relevant clause, FACT1948-S37, states: 'Where in any factory any manufacturing process produces dust, gas, fume or vapour of such character and to such extent as to be likely to explode on ignition, all practicable measures shall be taken to prevent any such explosion by effective enclosure of the plant or machinery, removal or prevention of accumulation of such dust, gas, fume or vapour, and exclusion or effective enclosure of all possible sources of ignition.' ... Work order WO-1003, which cleaned fouled intercooler tubes and reset a high-discharge-temperature trip, does not directly address the removal or prevention of accumulation of explosive dust, gas, fume, or vapour, or the exclusion or enclosure of ignition sources as mandated by FACT1948-S37. Thus, there is insufficient evidence to confirm compliance of C-201 with explosion-prevention requirements."* — opening sentence matches the required template almost verbatim; citations still `["FACT1948-S37", "WO-1003"]` (WO-1003 not dropped, per constraint); all 5 required concepts present (C-201, "explosion-prevention requirements", FACT1948-S37, WO-1003, a non-compliance determination).
- `python -m agents.validate_agents`: clean **8/8**, `compliance/CoQ2: ['FACT1948-S37', 'WO-1003']` exactly as expected — no regression from the prompt change.
- `python -m evaluation.score`-backed targeted RAGAS re-score via `agents.supervisor.answer()`: **blocked by Groq daily quota (429) on two separate attempts** (20:52 IST: 99,828/100,000 TPD used; 21:10 IST, after the reported ~16min cooldown: 99,835/100,000 TPD used, still 429). The near-identical "used" figures ~18 real minutes apart, despite Groq's error message quoting a ~15min retry window both times, indicate this is the **daily** cap (resets on a fixed daily boundary, not a rolling few-minute window) — Groq's per-call "try again in Xm" text is not a reliable countdown to actual reset. The `.env` key already carries the fresh key you provided earlier this session; it has now also hit its own daily ceiling (Groq's TPD limit is organization-level, not per-key, so rotating the key string doesn't grant new quota under the same org). No newer key has been provided to swap to.

**Resolved-or-open determination**: **structurally fixed and independently confirmed twice (answer text + citations unchanged across both quota-blocked attempts, `validate_agents` clean 8/8), but RAGAS numerical closure on `answer_relevancy` is still pending — genuinely blocked by quota, not a scoring failure or a regression. Per this task's own rule (never mark resolved without all three CoQ2 metrics ≥0.7, and never fabricate a number), this stays OPEN until a real `[SCORED]` run confirms it.** Faithfulness and context_precision are not expected to move (Round 3's context ordering/labeling is untouched by this round's change) but must still be re-confirmed ≥0.7 in the same run that scores `answer_relevancy`, not assumed carried over.

**Groq quota status**: org-level TPD cap effectively exhausted for the remainder of the current quota day as of this entry (2026-07-12 21:10 IST); next viable RAGAS attempt should wait for the actual daily reset rather than retrying on Groq's short-window error text.

## 2026-07-12 — `compliance/CoQ2` CLOSED: all three RAGAS metrics pass

`.env`'s `GROQ_API_KEY` was swapped to a fresh key (new org quota) after the round-4 entry above was written. Re-ran the same targeted CoQ2 verification (`agents.supervisor.answer()` for "Is compressor C-201 compliant with explosion-prevention requirements?") with no further code changes — this run scored cleanly instead of 429ing:

| metric | round-3 | round-4 (this run) | ≥0.7? |
|---|---|---|---|
| faithfulness | 0.89 (pass) | **0.75 (pass)** | Yes |
| context_precision | 1.00 (pass) | **1.00 (pass)** | Yes |
| answer_relevancy | 0.53 (amber) | **0.84 (pass)** | Yes |

Answer text (unchanged from the round-4 entry's structural check, now with a real score behind it): *"C-201 is not demonstrated compliant with explosion-prevention requirements under FACT1948-S37 based on the available context. The relevant clause, FACT1948-S37, states: 'Where in any factory any manufacturing process produces dust, gas, fume or vapour of such character and to such extent as to be likely to explode on ignition, all practicable measures shall be taken to prevent any such explosion by effective enclosure of the plant or machinery, removal or prevention of accumulation of such dust, gas, fume or vapour, and exclusion or effective enclosure of all possible sources of ignition.' This clause requires measures to prevent explosions, including the enclosure of plant or machinery and the exclusion of ignition sources. Work Order WO-1003, which cleaned fouled intercooler tubes and reset a high-discharge-temperature trip, does not directly address these requirements. Therefore, there is a gap in evidence for compliance with FACT1948-S37."* — citations `["FACT1948-S37", "WO-1003"]`, both retained; the finding remains truthfully non-compliant/insufficient-evidence, never flipped to falsely imply compliance.

`python -m agents.validate_agents`: clean **8/8**, `compliance/CoQ2: ['FACT1948-S37', 'WO-1003']` exactly as expected — no regression across any of the three rounds' fixes (context filtering, context ordering/labeling, opening-verdict phrasing).

**Resolved determination: CLOSED.** All three CoQ2 RAGAS metrics (faithfulness 0.75, context_precision 1.00, answer_relevancy 0.84) are ≥0.7 in the same scored run, `validate_agents` is clean, WO-1003 was never dropped, and no ground truth/threshold/`acceptable_intents` was loosened at any point across rounds 1–4. Carried-forward quality issue from Phase 6 sign-off is fully closed.

**Recap of what fixed each metric, for future reference**: faithfulness/context_precision were originally low (0.29/0.33) due to context pollution from unrelated `FailureEvent`/`Chunk` records — fixed by type-filtering `agents/compliance.py`'s context to `RegulatoryClause`/`Procedure`/`WorkOrder` only (round 1). `answer_relevancy` regressed to 0.00 next from RAGAS's `noncommittal` hard-zero gate on hedge-phrased "insufficient evidence" answers — fixed by requiring definite, named conclusions (round 2). `context_precision` then plateaued at 0.50 due to `LLMContextPrecisionWithoutReference`'s Average-Precision-over-retrieval-order mechanic penalizing `WO-1003` for being ranked before the accepted `FACT1948-S37` — fixed by clause-first context ordering + evidentiary-role labeling (round 3). `answer_relevancy` then plateaued at 0.53 due to `ResponseRelevancy` generating reverse-questions from the answer text alone, and the old hedge-style opening ("X's compliance turns on whether...") drifting the inferred question away from the real one — fixed by requiring a direct verdict-first opening sentence (round 4). No single fix would have closed all three; each targeted a distinct, independently-confirmed RAGAS internal mechanism.

Temp verification scripts (`_tmp_repro_coq2.py`, `_tmp_verify_coq2.py`, and their `.log` files) deleted now that the issue is closed.

## 2026-07-12 — `agents/validate_supervisor.py`: fail-open ground-truth loop

Tooling-only fix, unrelated to Compliance/CoQ2's own logic. The smoke-test run right after CoQ2 closed above hit this directly: `lessons_learned/LQ2` 429'd on Groq's daily quota, and because `_check_ground_truth()`'s loop had no `try/except` around `supervisor.answer()` (unlike `evaluation/validate_ragas.py`'s main loop, which already had this), the whole script aborted with an uncaught exception and exit 1 — losing the partial tally for the 7 queries (including `compliance/CoQ2`) that had already succeeded.

**Fix**: wrapped the `supervisor.answer()` call inside `_check_ground_truth()`'s loop in the same `try/except Exception` + `print(f"[CRASHED] {expected_intent}/{qid}: {exc}")` + `continue` pattern already used by `validate_ragas.py`. `total` is already incremented before the call, so a crashed query naturally counts against the final `matched/total` ratio without extra bookkeeping. Scope kept to this one loop (`_check_ambiguous`/`_check_fallback_edge` still crash-on-exception un-changed) — that's the loop with the tally the smoke-pass run actually needed; the other two checks don't accumulate a tally the same way, so widening this wasn't asked for and wasn't done.

**Verified**: `python -m py_compile agents/validate_supervisor.py` — syntax clean. Not re-run live against Groq (quota was already at ~99,400+/100,000 for the key in `.env` from the smoke-pass run moments earlier) — this is a pure error-handling change with no logic-path changes to routing/citation checks, so a live re-run isn't needed to trust the fix; it'll self-prove the next time `validate_supervisor` runs against a live quota-exhaustion event instead of guessing.

## 2026-07-12 — `agents.validate_supervisor` ground-truth loop: clean 8/8 confirmed

`.env`'s `GROQ_API_KEY` swapped again to a fresh key. Full `python -m agents.validate_supervisor` run: **`_check_ground_truth()`'s loop completed all 8 cases with zero `[CRASHED]`/`[MISMATCH]` entries — "8/8 clear-intent queries routed+answered correctly"**, including `compliance/CoQ2: routed=compliance citations=['FACT1948-S37', 'WO-1003']` matching exactly as expected. This is the clean run the CoQ2/validation thread was waiting on.

The script itself still exited 1 — but from `_check_ambiguous()`, which ran *after* the ground-truth loop finished and hit the same daily-quota 429 (this key was already down to ~99,300/100,000 TPD partway through the ground-truth loop itself, before `_check_ambiguous` even started — three separate "fresh" keys given this session have each shown near-exhausted quota almost immediately, suggesting the shared `on_demand` free tier itself is the bottleneck, not any individual key). Per this thread's own explicit decision, `_check_ambiguous`/`_check_fallback_edge` were deliberately left unhardened (should fail loudly, not swallow errors) — so this is expected behavior, not a bug: the one loop that needed partial-progress tolerance (ground-truth) got it and used it correctly; the two that should still hard-fail did.

**Status**: `compliance/CoQ2` closed (prior entries), `agents.validate_agents` clean 8/8 (prior entries), and now `agents.validate_supervisor`'s ground-truth check is also clean 8/8 in the same run. `_check_ambiguous`/`_check_fallback_edge` did not get a chance to run this pass (quota ran out first) — re-run the full script again once quota allows if a signed-off-clean full pass (all three checks) is wanted; the ground-truth number itself, the one this whole thread tracked, is confirmed.

## 2026-07-13 — RAGAS judge model switched to `qwen/qwen3-32b` (configurable)

**Reason**: the free-tier `llama-3.3-70b-versatile` 100,000-tokens/day quota repeatedly blocked smoke/RAGAS checks this whole CoQ2 thread — every "fresh" key handed in arrived already near-exhausted, and multiple `validate_supervisor`/`validate_ragas` runs 429'd mid-run. RAGAS's own judge calls (faithfulness statement-gen + verification, context precision, answer-relevancy question-gen) are the heaviest consumer of that pool, on top of the reasoning agents' and router's own usage of the same 70B/8B models.

**Change**: `evaluation/score.py`'s `JUDGE_MODEL` is now `os.environ.get("GROQ_JUDGE_MODEL", "qwen/qwen3-32b")` — configurable via `.env`, defaulting to Qwen3-32B. Everything else in `score_answer()` is untouched: `ChatGroq(model=JUDGE_MODEL, api_key=api_key, temperature=0)` and `bypass_n=True` are unchanged, `agents/llm.py`'s `REASONING_MODEL` (`llama-3.3-70b-versatile`) and `ROUTING_MODEL` (`llama-3.1-8b-instant`) are untouched — only the RAGAS judge moved, not the agents or the router.

**Model choice**: `qwen/qwen3-32b`, current Groq free-tier limit 500,000 TPD / 60 RPM — 5x the daily token budget of the old judge model, and now a separate pool from the agents'/router's own 70B/8B quota, so RAGAS scoring no longer competes with `validate_agents`/`validate_supervisor` for the same budget.

**Caveat**: Qwen3-32B is a preview model on Groq — scores from this judge are a **new evaluation baseline**, not directly comparable to the 70B-judge scores recorded earlier in this file (e.g. CoQ2's closing 0.75/1.00/0.84 above). No threshold or ground truth changed; the judge swap is the only thing that moved.

**Verified**:
- `python -m py_compile evaluation/score.py` — syntax clean.
- `python -m evaluation.score` self-check: **completed successfully** on the new judge, no quota error — `{'faithfulness': 0.5, 'context_precision': 0.9999999999, 'answer_relevancy': 0.975..., 'low_faithfulness': True}`. Numbers differ from any 70B-judge run of the same hardcoded example (expected — new baseline, not a regression check against old numbers).
- Targeted `compliance/CoQ2` RAGAS via `agents.supervisor.answer()`: **scored cleanly, no quota error** — `faithfulness: 0.9091 (pass)`, `context_precision: 1.0000 (pass)`, `answer_relevancy: 0.9558 (pass)`, citations unchanged (`['FACT1948-S37', 'WO-1003']`). All three metrics still clear 0.7 under the new judge — CoQ2's closed status holds under a second, independent judge model, not just the original 70B one.
- Full `evaluation.validate_ragas` (all 8 ground-truth queries): **not run this pass** — the two targeted checks above already confirm the new judge works end-to-end and CoQ2 survives it; a full 8-query baseline re-establishment under Qwen3-32B is worth doing as its own follow-up (new numbers for all 8 queries, not just CoQ2), not bundled into this config change.

## 2026-07-13 — `agents.validate_supervisor`: skip RAGAS entirely (quota isolation, not a scoring change)

**Why**: even after moving the judge to Qwen3-32B above, `validate_supervisor`'s routing/citation checks don't need RAGAS at all — they were only ever paying the judge-LLM cost as a side effect of reusing `supervisor.answer()`, and `_check_ambiguous`/`_check_fallback_edge` had never once completed this whole session because that judge-scoring cost (previously on the 70B pool) kept exhausting quota mid-run. This is a validation-tooling fix, not a product-scoring change: live answers must keep scoring by default.

**Change**: `agents.supervisor.answer(session, query, score: bool = True)` and `build_graph(session, score: bool = True)` — when `score=False`, agent nodes route straight to `END` instead of through the `score` node (no judge-LLM calls happen at all, not run-then-discarded), and `answer()` fills in `ragas_scores: {}`, `ragas_status: "skipped_disabled"`, `low_faithfulness: False` on the way out. `agents/state.py`'s `ragas_status` doc-comment updated to include the new value. `agents.validate_supervisor`'s three call sites (`_check_ground_truth`, `_check_ambiguous`, `_check_fallback_edge`) all now pass `score=False`. `/api/chat` (`backend/app/api/chat.py`) calls `answer()` with no `score` argument — untouched, still scores by default. No RAGAS threshold, judge model, ground truth, or agent prompt changed.

**Verified**:
- `python -m py_compile agents/supervisor.py agents/validate_supervisor.py` — clean.
- `python -m agents.validate_supervisor` — **completed fully for the first time this entire session**: ground-truth 8/8, all 3 ambiguous cases, and the fallback-edge check, with zero quota errors anywhere in the run.
- `python -m agents.validate_agents` — clean 8/8, unaffected (doesn't go through `supervisor.answer()` at all).
- `python -m agents.supervisor` (self-check, extended with a second `score=False` call + assertions) — both paths verified live: `score=True` returned `ragas_status=scored` with real numbers (`faithfulness=1.0, context_precision=0.68, answer_relevancy≈1.0`); `score=False` returned `ragas_status=skipped_disabled`, `ragas_scores={}`, `low_faithfulness=False`, and still a non-empty `agent_response`.

**Result**: the "leftover" item from the CoQ2 thread — a clean full `validate_supervisor` pass — is now closed, and closed in a way that doesn't depend on quota luck going forward (validation no longer competes with scoring for judge-LLM budget at all).

## 2026-07-13 — pre-demo checklist pass: backend/frontend/chat/telemetry verified live; found a real latency regression from the Qwen judge

Ran the actual pre-demo checklist end-to-end, not just the individual scripts: backend restarted (`uvicorn backend.app.main:app`, was down from an earlier session), frontend confirmed already up on `:3000`, one real `/api/chat` POST, one `/api/telemetry/scan` + `/api/telemetry/draft` flow.

**Backend/frontend**: both up and responding (`/api/equipment` → 200 OK in <1s).

**`/api/chat`** ("Why did P-101 fail in March 2025?"): full response shape confirmed — `agent_response`, `citations: ['FE-001', 'PROC-001', 'WO-1002']`, `graph_paths` (4 typed nodes), `routed_agent: rca`, `ragas_status: scored`, `ragas_scores: {faithfulness: 1.0, context_precision: 0.73, answer_relevancy: 0.92}`. Correct end-to-end.

**Telemetry**: `/scan` with `P-101` drifted toward `FE-001` (drift_pct=0.9) correctly matched (similarity 0.89) and produced a warning in ~3s. `/draft` from that match produced a real draft work order (`status: Draft`, description referencing the FE-001 pattern, `recommended_action: PROC-001 v2.1`) in ~11s. Both fast — no Groq/RAGAS involved, as expected.

**Finding, not a regression from anything touched today — a real cost of yesterday's Qwen judge-model swap**: a single scored `/api/chat` call took **~165–186 seconds** in direct testing (isolated, bypassing HTTP, to rule out a FastAPI-layer hang) and **~165s** again through the actual HTTP endpoint. This is not a hang or a bug — it completed successfully both times with correct scores — but it is a genuine latency cost: `evaluation/score.py`'s `bypass_n=True` (needed because Groq rejects `n>1`) turns Faithfulness's multi-statement verification into a sequence of individual judge calls, and Qwen3-32B's own per-call latency appears meaningfully higher than the old 70B model's. The quota problem is solved; a new UX/latency tradeoff was introduced in its place, and it wasn't caught until this live pre-demo check specifically exercised a real scored chat call end-to-end (prior verification of the Qwen swap only checked scores, not wall-clock time).

**Not fixed in this pass** — flagged, not resolved, since the user's own instruction was "no need to chase more model/eval plumbing right now." Documented here so it isn't silently discovered live during an actual demo. Two honest options for later, not decided here: accept the latency (RAGAS scoring is additive/fail-open, so a slow judge never blocks the answer being *shown*, but the score pills would visibly lag behind the answer in the UI by minutes) or set `GROQ_JUDGE_MODEL=llama-3.3-70b-versatile` back for the specific demo run if speed matters more than TPD headroom that day.

README's pre-demo checklist updated: item 4 notes `validate_supervisor` no longer touches RAGAS quota at all (`evaluation.validate_ragas` is the explicit quality-run command), item 5 clarifies the reasoning/router quota still applies independently of the judge-model change, item 6 is new (states the ~2.5–3 minute scored-chat latency plainly so it doesn't read as a hang), item 7 (server startup) now budgets for it.

## 2026-07-13 — demo-mode judge override: `GROQ_JUDGE_MODEL=llama-3.3-70b-versatile` in `.env`

**Why**: the prior entry's ~165–186s scored-chat latency under the `qwen/qwen3-32b` default is real but untenable live in front of an audience. `evaluation/score.py`'s `JUDGE_MODEL` is already env-configurable (yesterday's change) — used that override rather than touching code.

**Change**: added `GROQ_JUDGE_MODEL=llama-3.3-70b-versatile` to `.env` (commented to say why, and that removing the line restores the `qwen/qwen3-32b` default). No code changed. `qwen/qwen3-32b` stays the documented default in README/`evaluation/score.py` — this is a local `.env` override, not a default flip.

**Verified**: backend restarted (picked up the new env var — confirmed via a direct `dotenv` load showing `GROQ_JUDGE_MODEL=llama-3.3-70b-versatile` before testing), then one timed `/api/chat` call for "Why did P-101 fail in March 2025?":
- First attempt (2.6s) was a false read — a stale Neo4j connection from an unrelated transient AuraDB DNS blip during the backend restart, caught by `/api/chat`'s own 503 handler (`"Failed to read from defunct connection..."`), not a real scored answer. Not a code issue; AuraDB free-tier flakiness is already documented as a known recurring risk in this repo.
- Retry: **44.5 seconds**, real scored response — `citations: ['FE-001', 'PROC-001', 'WO-1002']`, `routed_agent: rca`, `ragas_status: scored`, `ragas_scores: {faithfulness: 0.80, context_precision: 0.29, answer_relevancy: 0.90}`. ~4x faster than the Qwen judge's measured 165–186s, and no quota error.

**Decision, per the condition set for this check**: 70B is neither too slow (44.5s is demo-viable) nor quota-blocked (scored cleanly) on this run, so `qwen/qwen3-32b` stays the documented high-quota default — this `.env` line is purely a same-day demo lever, not a default change. `context_precision` (0.29) reads low on this single run, consistent with the run-to-run judge variance already documented elsewhere in this file — not investigated further here, per this task's explicit "do not run full `evaluation.validate_ragas` before demo" scope, and not what this check was for (latency, not a quality re-baseline).

**Longer-term product fix, flagged not built**: don't block the answer on RAGAS at all — return `agent_response`/citations/`graph_paths` immediately, score asynchronously, and let the RAGAS strip update in place when scores land. That would make the judge-model latency (either judge) a non-issue for the demo's perceived responsiveness. Out of scope for today's low-risk env-override fix; a real change to `agents/supervisor.py`'s graph/`/api/chat`'s response contract and the frontend's `RagasStrip` polling/update behavior, not a config flip.
## 2026-07-13 - async RAGAS scoring for `/api/chat`

**Why**: the external validation report correctly exposed a product gap: the previous `/api/chat` path waited for RAGAS before the assistant message rendered, so the measured judge latency (44.5s with the 70B demo override, ~165-186s with the Qwen default) was user-visible answer latency. That made the report's "answer immediately, scores later" mitigation aspirational rather than true.

**Change**: `backend/app/api/chat.py` now calls `agents.supervisor.answer(..., score=False)` for the chat answer, then creates an in-memory RAGAS score job when retrieved context exists. The response returns `ragas_status: "scoring"` and `score_id` immediately; `backend/app/core/ragas_jobs.py` runs `evaluation.score.score_answer()` in a daemon thread and stores `scored`/`error` results for `GET /api/chat/scores/{score_id}`. The frontend (`frontend/lib/api.ts`, `frontend/components/ChatPanel.tsx`, `frontend/components/RagasStrip.tsx`) polls that endpoint and updates the selected message's score strip in place. Graph rendering now has the answer trail as soon as the answer returns; scoring lag no longer blocks chat/graph visibility.

**Scope/caveat**: the score-job store is in-memory and process-local, which is correct for this local hackathon demo but not a durable production queue. A backend restart drops pending score jobs; the frontend then surfaces `ragas_status: "error"` instead of blocking or hiding the answer. No RAGAS threshold, judge model, agent prompt, retrieval behavior, or ground truth changed.

**Verified**:
- `python -m py_compile backend/app/api/chat.py backend/app/core/ragas_jobs.py` - clean.
- `npm run lint` in `frontend/` - clean.
- `npm run build` in `frontend/` - clean.

## 2026-07-16 — Sprint 1 UX polish: responsive industrial console

Product-feel sprint, no new domain logic (no GraphRAG comparison, no CMMS/QMS writeback, no mem0 — per the sprint plan's own scope).

**Backend contract change (the only one)**: `POST /api/telemetry/scan` now also returns `reading_meta`, keyed by telemetry dimension, each `{label, unit, nominal}`. Source of truth is `telemetry/generator.py`'s new `DIMENSION_LABELS` + `meta_for()` — `nominal` is derived from the existing `NOMINAL` dict, not duplicated. `generator.py`'s `__main__` self-check extended with a `meta_for` assertion. No other endpoint contract changed; `/api/chat` was already returning `intent`/`routing_confidence`/`user_query` — the frontend just starts using them.

**Frontend rework**:
- `app/page.tsx` — responsive shell replacing the fixed 2x2 grid: desktop (lg) = chat left / evidence right / telemetry full-width below; tablet (md) = chat pinned + Evidence/Telemetry segmented switch; mobile = single-column Ask/Evidence/Telemetry segmented nav. Panels render exactly once (visibility is CSS breakpoint classes), so no duplicate NVL canvas or scan loop. Compact operational header.
- `components/ContextBar.tsx` (new) replaces the global `RagasStrip.tsx` (deleted): selected-answer context bar showing query snippet, routed agent, intent, routing confidence, citation count, and that answer's RAGAS status/scores.
- `components/ChatPanel.tsx` — explicit selected-message state (keyed on `score_id`, "driving evidence" marker); intent + confidence chips per assistant card; citation chips are now buttons — clicking selects the answer and (on tablet/mobile) switches to the Evidence view. Score polling: cancels on unmount via a ref guard, per-answer "scoring…" indicator, and a terminal `scoring_delayed` status after ~3 min (90 × 2s polls — sized to cover the measured ~2.5-3 min Qwen judge) instead of pending forever. `scoring_delayed` is a frontend-only status value (`lib/api.ts`'s `RagasStatus` union) — the backend never emits it.
- `components/GraphPanel.tsx` — legend keyed to the existing `TYPE_COLORS`, plus an evidence-details section (node/rel counts, entities grouped by type, selected-answer query snippet) under the NVL canvas. Keyed-result stale-fetch guard unchanged.
- `components/TelemetryPanel.tsx` — labeled explicitly as a simulation ("Not a live plant feed"); equipment change clears scan/preview/error at the event handler; request-version ref guard so an older scan response can never overwrite a newer one (rapid slider movement / equipment switch); explicit "scanning…" state; raw reading dump replaced with a metric grid driven by `reading_meta` (value + unit + nominal, deviation-banded coloring); Accept/Reject replaced with an honest editable preview — editable description + recommended action, `Mark reviewed` / `Dismiss preview` actions, and the visible note "Demo preview only — no work order is written back." Still no backend write path (enforced by omission, as before).
- `app/globals.css` — Geist sitewide (Arial override removed), global `:focus-visible` outline, dark palette kept.

**Verified**: `npm run lint` clean; `npm run build` clean (Next 16.2.10). Backend: `meta_for()` unit smoke; mocked-session TestClient smokes through the real endpoint code for `/api/telemetry/scan` (reading_meta exact-match, warning at full drift, none at nominal), `/api/graph` (nodes + relationship shaping), and `/api/chat` (intent/confidence/user_query pass-through, `score=False` + async score job creation, score-poll endpoint) — same mock technique as the Phase 6/8 fail-open proofs.

**Live-DB smoke blocked, environment not code**: the AuraDB hostname (`a2eb70e2.databases.neo4j.io`) no longer resolves — confirmed NXDOMAIN via public DNS, i.e. the free instance was paused/deleted, not a transient blip. Resume/recreate the instance and update `.env` before any live run. Separately, this machine's previous Python env is gone; a fresh `.venv` was created at repo root with all five requirements files installed — but **torch's DLLs are blocked by a Windows Application Control policy** in this venv (`WinError 4551` loading `torch_python.dll`), which blocks `sentence_transformers` imports (embeddings/rerank → Copilot path) until the policy allows it or torch is reinstalled in a trusted location. The smoke scripts stub `sentence_transformers` for exactly this reason; the live product needs it working for Copilot's hybrid retrieval.
